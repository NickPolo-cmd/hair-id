# Сплошная проверка кода — 10 сентября 2026

Проверка шла в два круга: девять независимых проверяющих по направлениям,
затем по каждому направлению отдельный агент пытался **опровергнуть** находки,
открывая файл и строку. Ниже — только то, что опровергнуть не удалось.

**Подтверждено дефектов: 76** — критичных 7, важных 45, мелочей 24.


## КРИТИЧНО

### 1. Панель заказа рисуется поверх открытого мобильного меню и делает все шесть ссылок ненажимаемыми

- **Где:** `site-shop/css/shop.css:657` (направление: адаптив)
- **Как ломается:** Телефон 375×667, страница site-shop/index.html. Кладём любой товар в корзину — catalog.js:398 ставит панели класс is-visible, она выезжает. Жмём бургер. Замер в браузере после окончания анимации: .cart-panel занимает y=137…559 (высота 422 px при одной позиции в заказе), а шесть пунктов меню стоят на y=192…519 — то есть целиком внутри панели. Проверка document.elementFromPoint по центру каждой ссылки возвращает элементы панели заказа (UL.cart-panel__list, LI.cart-item, P.cart-panel__total, P.cart-panel__note, A.btn btn-primary, A.btn btn-ghost) — ни одна из шести ссылок не доступна ни для чтения, ни для нажатия. Причина именно та, что описана: .site-header (shop.css:66) — position: sticky + z-index 100, он создаёт свой контекст наложения, поэтому z-index 200 у оверлея меню внутри шапки (shop.css:872) на странице работает как 100, а .cart-panel — отдельный <aside> прямо в body (site-shop/index.html:363) с z-index 200. Ни shop-ui.js, ни catalog.js панель при открытии меню не закрывают: во всём shop.css только 5 правил menu-is-open, и ни одно не трогает .cart-panel.
- **Починка:** Файл /Users/nick/Desktop/Волосы/site-shop/css/shop.css. Внутри существующего блока @media (max-width: 768px) сразу после строки 859 (`.cart-panel { right: 0; bottom: 108px; width: 100%; border-inline: none; }`) вставить две строки:

  body.menu-is-open .cart-panel,
  body.menu-is-open .mobile-bar { display: none; }

Это закрывает и находку №2 в части магазина. Ничего больше в файле не менять. После правки пересобрать копию docs/shop/css/shop.css (сейчас она отличается от site-shop/css/shop.css).

### 2. Заказ и панель корзины показывают «Итого 0 ₽»

- **Где:** `site-shop/js/catalog.js:449` (направление: js)
- **Как ломается:** Проверил своими глазами: products.data.js — 35 из 36 товаров с "price": null, единственная цена 1500 (grep дал ровно 35 null и одну 1500). Все четыре набора (строки 193, 202, 211, 220) тоже с price: null. money() на строке 240 возвращает «Цена по запросу» ТОЛЬКО для null и undefined, а cartTotal() (383–388) через `line && line.price ? … : 0` превращает бесценовые позиции в ноль, то есть на выходе получается число 0, и money(0) даёт «0 ₽». Действие пользователя: на site-shop/katalog.html нажать «В заказ» у любого средства кроме t013 — панель корзины (строка 418, элемент #cart-total, разметка katalog.html:165) печатает «Итого 0 ₽», а кнопка WhatsApp уходит с текстом «Итого: 0 ₽» (строка 449). При этом построчно в том же сообщении честно написано «цену уточню» — то есть само сообщение себе противоречит.
- **Починка:** В файле /Users/nick/Desktop/Волосы/site-shop/js/catalog.js: (1) строки 383–388, тело cartTotal() заменить на: `function cartTotal() { var ключи = Object.keys(cart); var безЦены = ключи.some(function (k) { var l = lineById(k); return !l || l.price === null || l.price === undefined; }); if (безЦены) return null; return ключи.reduce(function (sum, k) { var line = lineById(k); return sum + (line ? line.price * cart[k] : 0); }, 0); }`. (2) строку 418 `if (totalBox) totalBox.textContent = money(cartTotal());` заменить на `if (totalBox) { var т = cartTotal(); totalBox.textContent = т === null ? 'по запросу' : money(т); }`. (3) строку 449 `lines.push('Итого: ' + money(cartTotal()));` заменить на `var итог = cartTotal(); lines.push(итог === null ? 'Итого: цену уточню по позициям без цены' : 'Итого: ' + money(итог));`

### 3. При «уменьшить движение» на главной пропадают монограмма и надпись логотипа

- **Где:** `site/js/motion.js:375` (направление: js)
- **Как ломается:** Строка 375: `if (!первыйЭкран || reduced) return;` — при системной настройке «уменьшить движение» initLogoMorph выходит, logoMorph остаётся null, measureLogoMorph() (строки 384–427) не выполняется и ширину летящим элементам не выставляет. У .brandmark в site/css/main.css:312–325 стоит `width: 1px` (в находке был указан 279 — номер другой, правило то же), посадочные места в site/index.html:123–124 — пустые <span> без картинки и без фона (site/css/main.css:250–252). Одновременно правило site/css/main.css:329 `.has-motion body.has-hero-logo .header-logo img { visibility: hidden; }` лежит в main.css вне какого-либо @media (в motion.css такой медиазапрос есть, но это другое правило), а класс has-motion со страницы не снимается: страховка в site/index.html:39–42 снимает его только если через 3 с нет метки motion-ready, а motion.js ставит motion-ready на строке 586 — то есть до вызова initLogoMorph. Итог на site/index.html (единственная страница с body class="has-hero-logo"): монограммы и надписи нет ни в первом экране, ни в шапке — обе картинки .brandmark висят шириной 1 пиксель в левом верхнем углу, а картинки в шапке скрыты через visibility. Уточняю: подпись hero__logo-tagline (отдельный <img>) остаётся видимой, так что экран не пустой, но самого логотипа Hair ID нет нигде.
- **Починка:** В файле /Users/nick/Desktop/Волосы/site/js/motion.js строку 375 `if (!первыйЭкран || reduced) return;` заменить на две строки: `if (!первыйЭкран) return;` и `if (reduced) { body.classList.remove('has-hero-logo'); return; }`. Снятие класса отключает правило site/css/main.css:329 (оно требует body.has-hero-logo), и обычный логотип в шапке становится видимым. Ничего другого трогать не нужно: класс has-hero-logo стоит только в site/index.html:53.

### 4. Каталог существует только в JavaScript: в HTML нет ни одного товара

- **Где:** `site-shop/katalog.html:96` (направление: сео-техничка)
- **Как ломается:** Открыть исходник site-shop/katalog.html: на строке 96 стоит пустой контейнер <div class="shop-grid" id="shop-grid" data-reveal-group></div>, карточки создаёт js/catalog.js (подключается строкой 177) из js/products.data.js (строка 176) уже в браузере. Замерил видимый текст страницы без скриптов: 992 знака на всю страницу — шапка, заголовок «Уход домой: начните с задачи», два абзаца и футер. Ни одного названия товара, ни блоков «кому подходит / кому не подходит», ни цен. <noscript>-запасного варианта нет (grep по файлу — 0 совпадений). Отключите JS или посмотрите сохранённую копию страницы в поиске — витрины из 36 позиций там не будет; Яндекс индексирует JS-контент нестабильно и с задержкой, так что главная коммерческая страница магазина конкурирует одним заголовком.
- **Починка:** Тем же скриптом, который собирает site-shop/js/products.data.js (tools/товары-в-магазин2.mjs), дописывать готовую разметку карточек прямо в site-shop/katalog.html внутрь #shop-grid (строка 96): на каждый из 36 объектов — <article class="shop-card" id="t001" data-task="damaged"><h3>Коктейль масел: жожоба и макадамия</h3><p class="shop-card__vol">MOCHEQI MUSK, 45 мл</p><p>Неповторимый и дорогой аромат. Идеален для секущихся кончиков…</p></article>. В js/catalog.js функцию отрисовки сетки переключить на фильтрацию уже существующих в DOM карточек (показ/скрытие по data-task) вместо их создания, чтобы карточки не дублировались. После правки пересобрать docs.

### 5. На четырёх внутренних страницах студии не работает бургер-меню: не подключён js/picker.js

- **Где:** `site/uslugi.html:338` (направление: html-структура)
- **Как ломается:** Обработчик клика по .burger существует только в site/js/picker.js (initBurger, строки 133–148, вешает body.classList.toggle('menu-is-open')). Этот файл подключён единственный раз — site/index.html:1268. Блоки скриптов внутренних страниц: uslugi.html:338-342, raboty.html:286-291, o-mastere.html:189-193, kak-doehat.html:185-189 — в них analytics.js, motion.js, booking.js, sparticles, texture.js (+pages.js в raboty), picker.js нет. Инлайн-скрипт в head (uslugi.html:26-34) только про has-motion. Grep 'menu-is-open' по site/*.html не находит ничего, grep 'burger' по site/js/ находит только picker.js. Кнопка <button class="burger"> при этом есть: uslugi.html:73, raboty.html:73, o-mastere.html:73, kak-doehat.html:73. CSS site/css/main.css:1449-1453 (@media max-width:768px) ставит .site-nav и .header-contact в display:none и .burger в display:flex; main.css:1444 (@media max-width:1024px) прячет .header-phone. Экран: телефон, страница «Услуги и цены» (или «Работы», «Обо мне», «Как доехать») — жмём бургер, ничего не открывается, навигации и номера телефона нет, уйти можно только кнопкой «назад». Для сравнения: магазин делает это правильно — shop-ui.js с тем же initBurger подключён на всех пяти страницах.
- **Починка:** В /Users/nick/Desktop/Волосы/site/uslugi.html после строки 342 (<script src="js/texture.js"></script>) добавить строку <script src="js/picker.js"></script>. То же самое: site/raboty.html после строки 291, site/o-mastere.html после строки 193, site/kak-doehat.html после строки 189. Это безопасно: initPicker (picker.js:86-89) выходит, если нет [data-picker] и #picker-result, initBurger (picker.js:134-135) выходит, если нет .burger. После правки пересобрать docs/ (node tools/сборка-для-github.mjs https://nekitshit.github.io/hair-id).

### 6. Заказ уходит с суммой «Итого: 0 ₽» — позиции без цены просто пропускаются

- **Где:** `site-shop/js/catalog.js:386` (направление: магазин)
- **Как ломается:** В products.data.js 36 товаров, из них 35 с "price": null (проверено: grep -c '"price": null' = 35), цена есть только у t013 (строка 226, 1500). cartTotal() на строке 386 складывает `line && line.price ? line.price * cart[k] : 0`, то есть null-позиции дают 0, а money(0) на строке 240 возвращает не «Цена по запросу», а «0 ₽» (проверка `n === null || n === undefined` нулю не мешает). В панели заказа (строка 418, элемент id="cart-total") и в тексте, который уходит в WhatsApp (строка 449, «Итого: ») человек видит «Итого: 0 ₽» при полной корзине. Второй сценарий: t013 + любая позиция без цены → «Итого: 1 500 ₽», сумма занижена и выглядит как окончательная. Мастер получает заявку с неверной суммой.
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/js/catalog.js после функции cartTotal() (после строки 388) добавить:

  function hasUnpriced() {
    return Object.keys(cart).some(function (k) { var l = lineById(k); return !l || !l.price; });
  }
  function totalLabel() {
    if (!Object.keys(cart).length) return '—';
    if (!hasUnpriced()) return money(cartTotal());
    return cartTotal() > 0 ? 'от ' + money(cartTotal()) + ' + позиции по запросу' : 'цену подтвержу в ответе';
  }

Строку 418 `if (totalBox) totalBox.textContent = money(cartTotal());` заменить на `if (totalBox) totalBox.textContent = totalLabel();`
Строку 449 `lines.push('Итого: ' + money(cartTotal()));` заменить на `lines.push('Итого: ' + totalLabel());`
В пяти файлах site-shop (index.html:370, katalog.html:165, nabory.html:164, o-mastere.html:189, kak-vybrat.html:233) заменить `<b id="cart-total">0 ₽</b>` на `<b id="cart-total">—</b>`.
После правки пересобрать docs: node tools/сборка-для-github.mjs (docs/shop/js/catalog.js сейчас побайтово совпадает с исходником — diff -q молчит).

### 7. docs/ устарел: выложится версия сайта без новой логики логотипа

- **Где:** `docs/index.html:69` (направление: сборка-docs)
- **Как ломается:** Владелец выполняет шаг 2 из ВЫКЛАДКА-НА-GITHUB.md (git init && git add -A && git commit) и включает Pages на папке /docs. На живом сайте открывается главная: в шапке — статичная монограмма, на первом экране — старая картинка hair-id-mark.png, перелёта логотипа при прокрутке нет вообще (в docs/js/motion.js слова brandmark нет ни разу, в docs/css/main.css — тоже 0 совпадений). Правки владельца за 9 и 10 сентября пропадают.
- **Починка:** Перед первым коммитом выполнить: cd ~/Desktop/Волосы && node tools/сборка-для-github.mjs https://nekitshit.github.io/hair-id. Затем проверить тремя командами: ls docs/assets/logo/hair-id-wordmark.png docs/assets/logo/hair-id-tagline.png (оба должны найтись), grep -c brandmark docs/css/main.css docs/js/motion.js (должно быть больше нуля), find site site-shop -type f -newer docs/index.html (должно быть пусто).


## ВАЖНО

### 1. Нижняя липкая панель рисуется поверх полноэкранного меню и перехватывает нажатие по последнему пункту

- **Где:** `site/css/main.css:1347` (направление: адаптив)
- **Как ломается:** Телефон с высотой экрана 568 px (iPhone SE 1-го поколения, 320×568), site/index.html. Жмём бургер, меню открыто. Замер: .mobile-bar занимает y=445…568, пункт «Уход домой» — y=485…519. document.elementFromPoint по центру этого пункта возвращает A.mobile-bar__btn mobile-bar__btn--ghost, то есть кнопку «Позвонить». Человек метит в «Уход домой» (переход в магазин), а телефон открывает набор номера. Пункт «Как доехать» (426…461) закрыт нижней половиной. В горизонтальной ориентации (667×375) той же панелью перехвачены уже «Услуги и цены» (elementFromPoint → A.mobile-bar__btn) и «Работы» (→ DIV.mobile-bar). Механизм подтверждён: .site-header — position: fixed + z-index 100 (main.css:63-74) создаёт контекст наложения, поэтому z-index 200 у оверлея меню внутри шапки (main.css:1483) на странице действует как 100; .mobile-bar — прямой ребёнок body с тем же z-index 100 (main.css:1347), но стоит в разметке позже (site/index.html:1261 против </footer> на 1254), и при равном z-index побеждает порядок в документе. То же самое воспроизвёл в магазине при 320×568: пункт «Студия» (485…519) перехвачен кнопкой «Подбор».
- **Починка:** Два файла.
1) /Users/nick/Desktop/Волосы/site/css/main.css — в блоке «Мобильное меню» @media (max-width: 768px) после закрывающей скобки правила body.menu-is-open .site-nav (строка 1487) вставить строку:

  body.menu-is-open .mobile-bar { display: none; }

2) /Users/nick/Desktop/Волосы/site-shop/css/shop.css — та же строка добавляется вместе с починкой находки №1 (после строки 859), отдельно её дублировать не нужно.
После правки пересобрать docs/css/main.css и docs/shop/css/shop.css.

### 2. Полноэкранное меню не прокручивается — в горизонтальной ориентации три нижних пункта недостижимы

- **Где:** `site/css/main.css:1479` (направление: адаптив)
- **Как ломается:** Телефон повёрнут горизонтально, 667×375, site/index.html, меню открыто. Замер: у оверлея body.menu-is-open .site-nav высота ровно 375 px (inset: 0), computed overflow-y: visible, при этом scrollHeight = 519 и <ul> с пунктами тянется от y=192 до y=519. Пункты «Обо мне» (368…402), «Как доехать» (426…461) и «Уход домой» (485…519) лежат ниже нижней кромки окна. Прокрутки у фиксированного слоя нет; проверил window.scrollTo(0, 99999) — после прокрутки страницы в самый низ getBoundingClientRect последнего пункта остаётся top: 485, оверлей не сдвигается (его top: 0). До трёх пунктов не добраться никак, кроме поворота телефона обратно. Виноват верхний отступ padding: var(--space-11) = 192 px при высоте окна 375 px (main.css:1486). Тот же код в магазине: site-shop/css/shop.css:868-876.
- **Починка:** Два файла, правка одинаковая.
1) /Users/nick/Desktop/Волосы/site/css/main.css, строка 1486 — заменить
    padding: var(--space-11) var(--page-x) var(--space-8);
на три строки:
    padding: min(var(--space-11), 18vh) var(--page-x) var(--space-8);
    overflow-y: auto;
    -webkit-overflow-scrolling: touch;
2) /Users/nick/Desktop/Волосы/site-shop/css/shop.css, строка 875 — точно такая же замена (строка там дословно та же).
После правки пересобрать docs/css/main.css и docs/shop/css/shop.css.

### 3. На ширине ровно 768 px «выезжающий футер» включается вместе с мобильной раскладкой — верх футера не показывается ни при какой прокрутке

- **Где:** `site/css/motion.css:299` (направление: адаптив)
- **Как ломается:** Планшет в портрете, ровно 768×1024 (iPad, большинство Android-планшетов), site/index.html. Проверил matchMedia прямо в браузере: (max-width: 768px) → true И (min-width: 768px) → true одновременно. Из-за этого футер получает сразу мобильную раскладку в одну колонку (grid-template-columns вычисляется в единственное значение 691.203px, main.css:1456) плюс padding-bottom 192px (main.css:1474) — и вырастает до 1284 px при высоте окна 1024 px; и одновременно десктопный приём position: sticky; bottom: 0; z-index: 0 (motion.css:306-310) под непрозрачным .page-main с z-index: 1 (motion.css:300-304). Замер прокрутки: на максимальной прокрутке (scrollY = 29323 = максимум) getBoundingClientRect футера = top -260, bottom 1024, а блок .site-footer__brand (логотип Hair ID и строка «У каждых волос свой ID») = top -163. Прокрутил назад на 200, 400, 800 и 2000 px — top логотипа остаётся -163 при каждом положении, то есть верхние 260 px футера прибиты выше кромки окна и не показываются никогда. Для контроля тот же прогон на 767 px: футер становится position: static, и уже при прокрутке назад на 200 px логотип виден (top 37, elementFromPoint возвращает сам IMG).
- **Починка:** Файл /Users/nick/Desktop/Волосы/site/css/motion.css, строка 299 — заменить
@media (min-width: 768px) {
на
@media (min-width: 769px) {
Больше в файле ничего не трогать: это единственное место, где «выезжающий футер» пересекается с мобильным блоком main.css:1449. В site-shop правка не нужна — у магазина в index.html нет атрибута data-footer-reveal, приём там не включается. После правки пересобрать docs/css/motion.css.

### 4. Корзина обнуляется при переходе между страницами магазина

- **Где:** `site-shop/js/catalog.js:237` (направление: js)
- **Как ломается:** Строка 237: `var cart = {};` — обычная переменная внутри IIFE. grep по site/js, site-shop/js, docs/js, docs/shop/js по localStorage и sessionStorage даёт ноль совпадений, то есть корзина нигде не сохраняется. Магазин многостраничный и связан обычными ссылками: в site-shop/katalog.html:59–60 и 116–117 стоят ссылки на katalog.html и nabory.html, сетка товаров (#shop-grid) есть на katalog.html, блок наборов (#shop-sets) — на nabory.html, панель корзины продублирована на пяти страницах. Действие пользователя: в «Каталоге» добавить два средства, перейти по ссылке «Наборы», добавить набор — на nabory.html скрипт стартует заново, cart пустой, первые две позиции исчезли без предупреждения, и в WhatsApp уходит заказ только на набор.
- **Починка:** В файле /Users/nick/Desktop/Волосы/site-shop/js/catalog.js: (1) после строки 237 добавить `function сохранитьКорзину() { try { localStorage.setItem('hairid-cart', JSON.stringify(cart)); } catch (e) {} }` и `function прочитатьКорзину() { try { var с = JSON.parse(localStorage.getItem('hairid-cart') || '{}'); if (с && typeof с === 'object' && !Array.isArray(с)) { Object.keys(с).forEach(function (k) { if (typeof с[k] === 'number' && с[k] > 0 && lineById(k)) cart[k] = с[k]; }); } } catch (e) {} }`. (2) в add() (строки 456–460) перед `renderCart();` вставить `сохранитьКорзину();`. (3) в init() (строка 531) первой строкой вызвать `прочитатьКорзину();` — до renderProducts()/renderCart(). try/catch обязателен: в приватном режиме Safari обращение к localStorage бросает исключение.

### 5. Добавление в заказ захлопывает раскрытый блок «Почему это работает»

- **Где:** `site-shop/js/catalog.js:437` (направление: js)
- **Как ломается:** renderCart() последней строкой (437) вызывает renderProducts(), а тот на строке 329 переписывает innerHTML контейнера #shop-grid целиком. Карточки собираются шаблоном заново, и <details class="product__more"> на строке 339 всегда создаётся без атрибута open — состояние не переносится. Действие пользователя на site-shop/katalog.html: раскрыть «Почему это работает» у средства, прочитать состав, нажать «В заказ» (кнопка на строке 344, обработчик через add() → renderCart() → renderProducts()) — сетка перерисовывается, раскрытый блок закрывается, карточки становятся ниже и содержимое подпрыгивает вверх. То же самое при каждом нажатии «+» и «−» в панели корзины (строки 484 и 487 ведут в тот же add()). Показательно, что для наборов авторы эту проблему уже обошли: строки 430–435 обновляют только подпись кнопки и класс, без перерисовки.
- **Починка:** В файле /Users/nick/Desktop/Волосы/site-shop/js/catalog.js строку 437 `renderProducts();` (последняя строка renderCart) заменить на блок по образцу строк 430–435: `document.querySelectorAll('[data-add]').forEach(function (b) { var id = b.getAttribute('data-add'); b.textContent = cart[id] ? 'В заказе' : 'В заказ'; var card = b.closest('.product'); if (card) card.classList.toggle('is-in-cart', !!cart[id]); });` Полная перерисовка сетки останется там, где она нужна по смыслу — при смене фильтра задач (строки 472 и 533).

### 6. Опубликованная копия docs отстала от исходников site

- **Где:** `docs/js/motion.js:366` (направление: js)
- **Как ломается:** Сравнил файлы: docs/js/motion.js:365–370 — старый движок, ищет [data-logo-hero] и [data-logo-header], тогда как site/js/motion.js:367–377 работает через .brandmark--sign / .hero__logo-slot--sign. Расходятся и другие файлы: docs/index.html (34 строки разницы, brandmark--sign — 0 совпадений против 1 в site), docs/css/main.css (144 строки разницы). Даты изменения прямо это подтверждают: docs собран 9 сентября в 20:23, а site/js/motion.js и site/css/main.css правились 10 сентября в 13:55 и 14:03. Внутри себя docs согласован (старый HTML + старый JS + старый CSS) и на живом сайте не падает, но это снимок вчерашнего состояния: всё, что чинится в site/ и site-shop/ — включая находки 1–5 из этого списка — на nekitshit.github.io/hair-id не попадёт. Отличие docs/js/analytics.js от site/js/analytics.js — одна строка комментария с адресом (netlify → nekitshit.github.io), это осознанная замена, а не отставание.
- **Починка:** Пересобрать /Users/nick/Desktop/Волосы/docs заново из site/ (в корень docs) и site-shop/ (в docs/shop/) тем же скриптом, что делал прошлую сборку, сохранив единственную осознанную правку — замену адреса netlify на nekitshit.github.io/hair-id в js/analytics.js (строка 8) и в shop/js/shop-ui.js. После выкладки проверить страницу запросом заголовков: `node /Users/nick/.claude/tools/fetch.mjs --head https://nekitshit.github.io/hair-id/` (ожидается HTTP 200) и открыть её в браузере владельца.

### 7. docs/ — устаревшая сборка: разметка логотипа старая, а двух PNG логотипа в сборке нет

- **Где:** `docs/index.html:69` (направление: ссылки)
- **Как ломается:** Экран: главная страница опубликованного сайта, шапка и первый экран. В site/index.html:58-61 живёт новая перелетающая монограмма из двух элементов (.brandmark--sign со знаком и .brandmark--word с надписью), в site/index.html:77-78 — пара картинок в шапке, в site/index.html:125-126 — hair-id-tagline.png в первом экране. В docs/index.html на строке 69 стоит старый вариант: одна картинка <img width="320" height="232" src="assets/logo/hair-id-monogram.png" alt="Hair ID"> и больше ничего. Слово brandmark: site/index.html — 2 вхождения, site/css/main.css — 1, site/js/motion.js — 2; docs/index.html, docs/css/main.css, docs/js/motion.js — 0. diff -q подтверждает, что docs/css/main.css и docs/js/motion.js отличаются от исходных. Даты: docs собран 09.09 20:23, site/index.html правлен 09.09 20:33, site/css/main.css — 10.09 13:54, site/js/motion.js — 10.09 13:55. Файлы hair-id-wordmark.png (43 КБ) и hair-id-tagline.png (20 КБ) созданы 09.09 20:33, то есть уже после сборки: в site/assets/logo/ они есть, в docs/assets/logo/ и docs/shop/assets/logo/ их нет. Действие пользователя, при котором ломается: владелец делает первый коммит и включает GitHub Pages — посетитель получает главную с логотипом до правки, без перелёта монограммы и со вчерашними CSS и JS. Второй сценарий, ещё нагляднее: кто-то вручную переносит новую разметку логотипа в docs/index.html, не копируя картинки, — и в шапке сразу два 404, hair-id-wordmark.png и hair-id-tagline.png.
- **Починка:** Пересобрать папку docs целиком одной командой из корня проекта: cd "/Users/nick/Desktop/Волосы" && node tools/сборка-для-github.mjs https://nekitshit.github.io/hair-id (эта же строка записана в шапке самого скрипта tools/сборка-для-github.mjs и в ВЫКЛАДКА-НА-GITHUB.md, шаг 1). Скрипт заново копирует site/ в корень docs/ и site-shop/ в docs/shop/, попутно переписывая адреса, поэтому недостающие hair-id-wordmark.png и hair-id-tagline.png приедут вместе с новой разметкой. После прогона проверить три вещи: test -f "/Users/nick/Desktop/Волосы/docs/assets/logo/hair-id-wordmark.png" и test -f "/Users/nick/Desktop/Волосы/docs/assets/logo/hair-id-tagline.png" — оба должны быть на месте; grep -c brandmark "/Users/nick/Desktop/Волосы/docs/index.html" — должно быть 2, а не 0; в выводе скрипта строка про старые адреса должна показать 0. Отдельно убрать пустой каталог-артефакт "/Users/nick/Desktop/Волосы/docs/shop 2" (удалять через node /Users/nick/.claude/tools/trash.mjs "/Users/nick/Desktop/Волосы/docs/shop 2", а не rm -rf).

### 8. Панель заказа спрятана только сдвигом — её кнопки остаются в табуляции за экраном

- **Где:** `site-shop/css/shop.css:652` (направление: доступность)
- **Как ломается:** На katalog.html при пустом заказе `.cart-panel` (shop-css:652–665) скрыта единственным способом — `transform: translateY(140%)` (строка 663); показ идёт классом `.is-visible` (строка 666). Grep по всей папке site-shop не находит для `.cart-panel` ни `display:none`, ни `visibility`, ни `inert`, ни `aria-hidden` (единственное второе правило — `css/shape.css:98`, оно задаёт только border-radius; в медиазапросе `css/shop.css:859` transform не переопределяется). Панель лежит после `</footer>` (katalog.html:158, nabory.html:157, o-mastere.html:182, kak-vybrat.html:226, index.html:363) и всегда содержит три фокусируемых элемента: кнопку «Свернуть» (katalog.html:161), ссылки «Заказать в WhatsApp» и «Telegram» (katalog.html:168–169). Человек с клавиатуры доходит Tab до конца страницы и получает три остановки фокуса на элементах, которых нет на экране; панель `position: fixed`, подкрутить её в вид браузер не может. То же после нажатия «Свернуть» — shop-ui.js:52 снимает только `is-visible`.
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/css/shop.css строки 663–666 заменить
```
  transform: translateY(140%);
  transition: transform 0.55s var(--ease-out-soft);
}

.cart-panel.is-visible { transform: translateY(0); }
```
на
```
  transform: translateY(140%);
  visibility: hidden;
  transition: transform 0.55s var(--ease-out-soft), visibility 0s 0.55s;
}

.cart-panel.is-visible {
  transform: translateY(0);
  visibility: visible;
  transition: transform 0.55s var(--ease-out-soft), visibility 0s 0s;
}
```
Ту же замену повторить в собранной копии /Users/nick/Desktop/Волосы/docs/shop/css/shop.css — там этот же блок лежит на строках 643–647.

### 9. Контраст 1,91:1 у примечания в блоке «После процедуры»

- **Где:** `site/css/main.css:1721` (направление: доступность)
- **Как ломается:** `.aftercare__note` (main.css:1716–1722) красится в `var(--c-text-subtle)` кеглем `--fs-caption` = 13px. Секция `#aftercare` помечена `data-theme="dark"` (index.html:756), в тёмной теме `--c-text-subtle: var(--green-400)` = #47584d (tokens.css:356). Подложка карточки — `color-mix(in srgb, var(--green-600) 55%, transparent)` (main.css:1676) поверх `[data-theme='dark'].section { background: var(--gradient-dark) }` (main.css:53), то есть #202c26 в середине градиента и #24302a у верхнего края. Пересчитал по формуле WCAG сам: #47584d к #202c26 даёт 1,91:1, к #24302a — 1,81:1 при норме 4,5:1. Текст «Полный список отдаю на руки после визита — запоминать не нужно» (index.html:800–801) на экране практически не читается.
- **Починка:** В /Users/nick/Desktop/Волосы/site/css/main.css строка 1721: заменить `  color: var(--c-text-subtle);` на `  color: var(--c-text-muted);`. В тёмной теме это #a9b3a9 (tokens.css:355), контраст на той же подложке 6,70:1 — пересчитал, сходится.

### 10. Открытое мобильное меню не удерживает фокус и не закрывается по Escape

- **Где:** `site/js/picker.js:137` (направление: доступность)
- **Как ломается:** `initBurger()` в picker.js вешает только `click` (строки 137–140): переключает `menu-is-open` и `aria-expanded`. Ни `keydown`, ни `inert`, ни блокировки прокрутки нет; то же в site-shop/js/shop-ui.js:33–36. При этом на ширине ≤768px `.site-nav` по умолчанию `display: none` (main.css:1450–1451), а с классом становится непрозрачной шторкой `position: fixed; inset: 0; background: var(--green-900)` (main.css:1479–1487; в магазине — shop.css:868–876). Содержимое под шторкой из DOM не убирается. Воспроизведение: сузить окно до 375px, открыть меню бургером, нажать Tab семь раз — после последнего пункта фокус уходит на ссылки главной страницы и на липкую панель `.mobile-bar` (main.css:1473), полностью закрытые сплошным зелёным фоном. Индикатор фокуса не виден нигде, вернуться к бургеру можно только протабав всю страницу.
- **Починка:** В /Users/nick/Desktop/Волосы/site/js/picker.js внутри `initBurger()` после блока строк 137–140 добавить:
```
    document.addEventListener('keydown', function (e) {
      if (e.key !== 'Escape' || !document.body.classList.contains('menu-is-open')) return;
      document.body.classList.remove('menu-is-open');
      burger.setAttribute('aria-expanded', 'false');
      burger.focus();
    });
```
и там же, в обработчике клика, после переключения класса добавить `['main', '.site-footer', '.mobile-bar'].forEach(function (s) { var el = document.querySelector(s); if (el) el.toggleAttribute('inert', open); });`
Ту же вставку сделать в /Users/nick/Desktop/Волосы/site-shop/js/shop-ui.js в `initBurger()` после строк 33–36.

### 11. Заголовок «Нельзя первые трое суток» даёт контраст 2,97:1

- **Где:** `site/css/main.css:1690` (направление: доступность)
- **Как ломается:** `.aftercare__col--no h3 { color: var(--c-danger); }` (main.css:1690) — это #a85c50 (tokens.css:170); `.aftercare__col--yes h3 { color: var(--c-success); }` (main.css:1689) — #6f8f6a (tokens.css:168). Подложка та же, что в находке 2: #202c26. Пересчитал сам: #a85c50 → 2,97:1, #6f8f6a → 4,01:1. Кегль `--fs-h4` = clamp(1.15rem, 1.6vw, 1.375rem) = 18,4–22px при `--fw-semibold` (main.css:1680–1687) — до порога «крупного текста» (24px, либо 18,66px при весе 700) не дотягивает. Заголовок «Нельзя первые трое суток» (index.html:779) — ключевой текст раздела, и он проваливает даже смягчённый порог 3:1 для крупного текста.
- **Починка:** В /Users/nick/Desktop/Волосы/site/css/tokens.css внутри блока `[data-theme='dark']` (начинается на строке 346) после строки 357 `  --c-text-inverse: var(--green-700);` добавить две строки:
```
  --c-success: #8fae89;
  --c-danger: #d08b7e;
```
Пересчитал: на подложке #202c26 это 5,92:1 и 5,29:1, на светлом краю градиента #24302a — 5,61:1 и 5,01:1. Светлую тему это не трогает, точки-маркеры `li::before` (main.css:1713–1714) осветлятся заодно и хуже не станут.

### 12. role="radiogroup" без единого role="radio": выбранное время не объявляется

- **Где:** `site/index.html:1155` (направление: доступность)
- **Как ломается:** `#booking-slots` объявлен `role="radiogroup"` (строка 1155), а четыре кнопки внутри (строки 1156–1159) остаются обычными `<button>` без `role="radio"`. `pickOne()` в site/js/booking.js:75–79 ставит на них `aria-checked`, но для роли `button` этот атрибут по ARIA не поддерживается и отбрасывается (для кнопок предусмотрен `aria-pressed`). Для дней всё сделано правильно — booking.js:60 создаёт кнопки уже с `role="radio" aria-checked="false"`. Воспроизведение: со скринридером выбрать «Вечер 16:00–20:00» — подтверждения выбора не будет, а вернувшись к группе позже, узнать активный слот нельзя: состояние живёт только в классе `.is-active` (main.css:1282).
- **Починка:** В /Users/nick/Desktop/Волосы/site/index.html в каждой из строк 1156–1159 после `type="button"` дописать ` role="radio" aria-checked="false"` — ровно так, как это уже сделано для дней в site/js/booking.js:60. Дальше pickOne() отработает сам.

### 13. Кнопка «Заказ» в шапке магазина не делает ничего

- **Где:** `site-shop/js/catalog.js:491` (направление: доступность)
- **Как ломается:** Обработчик `#cart-toggle` переключает `is-open`: `document.getElementById('cart-panel').classList.toggle('is-open')` (строка 491). Grep по /Users/nick/Desktop/Волосы/site-shop/css не находит ни одного правила с `.is-open` — только `body.menu-is-open` для мобильного меню (shop.css:868–887). Панель показывается исключительно классом `is-visible` (shop.css:666), который ставит renderCart по числу позиций (catalog.js:398). Так же мёртв класс `is-dismissed` из shop-ui.js:54 и 59 — правил под него в CSS нет. `.cart-toggle` (shop.css:144) ни в одном медиазапросе не скрывается, то есть кнопка «Заказ 0» видна всегда (katalog.html:69). Воспроизведение: открыть katalog.html, нажать «Заказ» — ноль реакции; `aria-expanded`/`aria-controls` у кнопки тоже нет. И после нажатия «Свернуть» (shop-ui.js:51–55) вернуть панель этой кнопкой нельзя — только добавив ещё один товар.
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/js/catalog.js строку 491 заменить
```
        document.getElementById('cart-panel').classList.toggle('is-open');
```
на
```
        var p = document.getElementById('cart-panel');
        p.classList.remove('is-dismissed');
        var open = p.classList.toggle('is-visible');
        toggle.setAttribute('aria-expanded', open ? 'true' : 'false');
```
и в /Users/nick/Desktop/Волосы/site-shop/katalog.html строку 69 (и такие же строки index.html:88, nabory.html:69, o-mastere.html:69, kak-vybrat.html:69) дополнить: `<button type="button" class="cart-toggle" id="cart-toggle" aria-expanded="false" aria-controls="cart-panel">`. Ту же правку повторить в собранной копии /Users/nick/Desktop/Волосы/docs/shop/.

### 14. outline: none у полей формы записи — фокус остаётся волосяной рамкой в 1 px

- **Где:** `site/css/main.css:1210` (направление: доступность)
- **Как ломается:** `.booking-form__select:focus, .booking-form__input:focus { border-color: var(--c-accent); outline: none; }` (main.css:1209–1210). Специфичность `.booking-form__input:focus` (0,2,0) выше, чем у глобального `:focus-visible { outline: 2px solid var(--c-accent); outline-offset: 3px; }` (tokens.css:464–467), поэтому общий контур снимается гарантированно, и правила `.booking-form__input:focus-visible` в файле нет. Секция `#booking` (index.html:1099) идёт без `data-theme`, то есть в светлой теме: рамка `1px solid var(--c-border)` = #e6dfd1 (main.css:1201, tokens.css:64) на фоне поля `--c-bg-deep` = #f2ede3, при фокусе становится `--c-accent` = #9a7b44. Воспроизведение: табать по форме записи (услуга → день → время → имя → длина → комментарий) — на всём остальном сайте фокус помечен золотым контуром 2px с отступом 3px, а внутри главной формы записи он пропадает, и на textarea высотой 92px единственный признак — потемневшая волосяная линия по краю.
- **Починка:** В /Users/nick/Desktop/Волосы/site/css/main.css строки 1209–1210 заменить
```
.booking-form__select:focus,
.booking-form__input:focus { border-color: var(--c-accent); outline: none; }
```
на
```
.booking-form__select:focus,
.booking-form__input:focus { border-color: var(--c-accent); }

.booking-form__select:focus-visible,
.booking-form__input:focus-visible { outline: 2px solid var(--c-accent); outline-offset: 2px; }
```
Ту же правку повторить в собранной копии /Users/nick/Desktop/Волосы/docs/css/main.css.

### 15. Результат подбора пишется в контейнер, пока он скрыт атрибутом hidden

- **Где:** `site/js/picker.js:113` (направление: доступность)
- **Как ломается:** `#picker-result` в разметке помечен `hidden aria-live="polite"` (site/index.html:323). В обработчике сначала заполняется содержимое — `result.innerHTML = ...` (picker.js:103–111), и только потом снимается скрытие — `result.hidden = false` (строка 113). Пока стоит `hidden`, элемент имеет `display:none` и в дереве доступности отсутствует целиком, поэтому живой области, внутри которой могла бы произойти мутация, в этот момент просто нет. Фокус на новый блок не переводится: код только прокручивает страницу, и то лишь если `rect.bottom > window.innerHeight` (строки 119–124). Воспроизведение: со скринридером нажать карточку «Волосы после осветления» — ответ на несколько абзацев с кнопкой «Записаться на диагностику» появляется ниже, а озвучивания нет.
- **Починка:** В /Users/nick/Desktop/Волосы/site/js/picker.js перенести строку 113 `        result.hidden = false;` выше — поставить её непосредственно перед строкой 103 `        result.innerHTML =`, и удалить прежнюю строку 113. Тогда живая область уже находится в дереве доступности к моменту записи содержимого. Ту же правку повторить в /Users/nick/Desktop/Волосы/docs/js/picker.js.

### 16. ItemList перечисляет 12 товаров из образца, которых в магазине нет

- **Где:** `site-shop/index.html:426` (направление: сео-техничка)
- **Как ломается:** Открыть site-shop/katalog.html: витрина рисуется из js/products.data.js — 36 товаров MOCHEQI MUSK/OLORCHEE («Коктейль масел: жожоба и макадамия», «Кератиновая маска Golden Series…»). Разметка ItemList на katalog.html:213-233 (и index.html:423-443, nabory.html:212-232, kak-vybrat.html:281-301, o-mastere.html:237-257) называет совсем другие 12 позиций. Проверено: js/catalog.js:248-263 перезаписывает переменную PRODUCTS содержимым window.HAIRID_PRODUCTS, поэтому образец из catalog.js:28-183 на страницу не попадает никогда; grep по products.data.js даёт 0 совпадений на «бессульфатн» и 0 на «липидн». Итог: структурированные данные описывают контент, которого на странице нет — прямое нарушение требования Google «размеченное должно быть видно пользователю».
- **Починка:** 1) На четырёх страницах, где витрины нет, удалить блок ItemList целиком: site-shop/index.html строки 423-443, site-shop/nabory.html 212-232, site-shop/kak-vybrat.html 281-301, site-shop/o-mastere.html 237-257 (вместе со <script type="application/ld+json"> и </script>). 2) В site-shop/katalog.html заменить блок 213-233 на ItemList, собранный из products.data.js: {"@context":"https://schema.org","@type":"ItemList","name":"Каталог средств Hair ID Shop","numberOfItems":36,"itemListElement":[{"@type":"ListItem","position":1,"item":{"@type":"Product","name":"Коктейль масел: жожоба и макадамия","image":"https://hairid-shop.netlify.app/assets/photo/tovary/t001.webp","url":"https://hairid-shop.netlify.app/katalog.html#t001"}}, …]} — по элементу на каждый объект массива. Адреса в исходниках пишутся на hairid-shop.netlify.app (так во всех остальных абсолютных ссылках site-shop), на github.io их переписывает tools/сборка-для-github.mjs. 3) Генерацию встроить в tools/товары-в-магазин2.mjs, чтобы список не расходился при следующей выгрузке. После правки пересобрать docs.

### 17. FAQPage не совпадает с вопросами на странице: 6 из 9, два вопроса выдуманы

- **Где:** `site/index.html:1322` (направление: сео-техничка)
- **Как ломается:** Открыть главную, раздел «Что обычно спрашивают»: девять <details> на строках 957, 965, 972, 979, 987, 995, 1002, 1012, 1021. В блоке FAQPage (1322-1359) шесть вопросов, и текст не совпадает: строка 1339 «Можно ли сделать кератин дома самой?» — такого вопроса на странице нет вообще (ближайший на 1012 звучит «А если просто купить средство и делать дома?»); 1344 «Чем ботокс для волос отличается от кератина?» против видимого на 965 «Чем ботокс отличается от кератина?»; 1349 «Сколько держится результат процедуры?» против 972 «Сколько держится результат?»; 1354 «Можно ли делать процедуры во время беременности?» против 987 «Можно во время беременности?». Ответы в разметке пересказаны от третьего лица («Составы без формальдегида, не чаще трёх раз в год»), на странице — от первого («Я работаю составами без формальдегида, делаю не чаще трёх раз в год»). Требование Google к FAQPage — вопрос и ответ должны присутствовать на странице в том же виде; при расхождении расширенный сниппет не показывается, а размеченный отсутствующий вопрос попадает под запрет на разметку невидимого контента.
- **Починка:** В /Users/nick/Desktop/Волосы/site/index.html заменить блок строк 1322-1359 на FAQPage из всех девяти вопросов, где name — посимвольная копия <summary>, а text — склеенный текст <p> того же <details> (строки 957-960, 965-968, 972-976, 979-984, 987-992, 995-999, 1002-1010, 1012-1018, 1021-1023). Первый элемент: {"@type":"Question","name":"Кератин вредит волосам?","acceptedAnswer":{"@type":"Answer","text":"Вредят неподходящий состав, нарушенная технология и слишком частое повторение. Я работаю составами без формальдегида, делаю не чаще трёх раз в год на одних волосах и проветриваю кабинет. Ломкость, которую приписывают кератину, обычно остаётся от осветления до него."}}. Лучше собирать этот JSON скриптом из самого HTML, чтобы тексты не расходились при следующей правке.

### 18. В makesOffer пропущена флагманская услуга, а цены «от» размечены как точные

- **Где:** `site/index.html:1306` (направление: сео-техничка)
- **Как ломается:** На главной 12 услуг (index.html, карточки с ценами; полный прайс — uslugi.html:115, 129, 143, 157, 171, 185, 199, 213, 227, 241, 255, 269). В makesOffer (index.html:1306-1318) одиннадцать Offer: нет самой дорогой и первой в списке позиции «Спасу ваши волосы» — она на index.html:370 с ценой «от 5 000 ₽» на строке 373 и на uslugi.html:114-115. Второе: у каждого Offer стоит "price": "3000" — по schema.org это точная цена, тогда как на странице «от 3 000 ₽», а на index.html:530-534 к базовой цене прибавляются +500 за среднюю длину, +1 000 за длинную и +500 за густоту. Машиночитаемая цена противоречит прайсу в том же файле и обещанию «цена названа заранее и на месте не растёт».
- **Починка:** В /Users/nick/Desktop/Волосы/site/index.html в блоке makesOffer (строки 1306-1318) заменить каждый элемент вида { "@type": "Offer", "priceCurrency": "RUB", "price": "3000", "itemOffered": … } на форму с минимальной ценой: { "@type": "Offer", "priceCurrency": "RUB", "priceSpecification": { "@type": "PriceSpecification", "priceCurrency": "RUB", "minPrice": 3000 }, "itemOffered": … } и первым пунктом добавить пропущенную услугу с minPrice 5000 и "name": "Спасу ваши волосы". Значения minPrice по порядку прайса uslugi.html: 5000, 3000, 3500, 3200, 3000, 1800, 2000, 1200, 2500, 800, 2500, 4000.

### 19. description магазина 222 знака и начинается с товаров, которых в каталоге нет

- **Где:** `site-shop/index.html:7` (направление: сео-техничка)
- **Как ломается:** Строка 7 — 222 знака, обрежется примерно после «…уход за жирной кожей головы, масла, терм…», и главный аргумент «Каждое средство отобрано мастером: кому подходит и кому нет» в сниппет не попадёт. Хуже вторая часть: описание открывается словами «Бессульфатные шампуни после кератина», а в js/products.data.js слово «сульфат» не встречается ни разу (grep -ic = 0) — среди 36 товаров сульфатной пометки нет ни у одного. Человек приходит по запросу «бессульфатный шампунь», видит подтверждение обещания ещё и в видимом тексте (index.html:154, katalog.html:88), листает витрину из шампуней и масел MOCHEQI MUSK и уходит.
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/index.html строку 7 заменить на: <meta name="description" content="Шампуни, маски, масла и термозащита, отобранные мастером Hair ID: кому подходит и кому нет. Бесплатный подбор по фото. СПб и Ленинградская область."> — 147 знаков. Слово «бессульфатный» убрать также из keywords (строка 8) и из видимого текста на index.html:154 и katalog.html:88 либо, если товары действительно бессульфатные, проставить это в карточках products.data.js.

### 20. В картах сайта обеих площадок отданы страницы с meta robots noindex

- **Где:** `site/sitemap.xml:4` (направление: сео-техничка)
- **Как ломается:** site/sitemap.xml строки 4 и 5 отдают privacy.html и terms.html; site-shop/sitemap.xml строки 4 и 5 — terms.html и privacy.html. Во всех четырёх страницах в head стоит <meta name="robots" content="noindex, follow"> (site/privacy.html:8, site/terms.html:8, site-shop/privacy.html:8, site-shop/terms.html:8). После отправки карты Google Search Console выдаёт по этим адресам ошибку «Отправленный URL содержит тег noindex», Яндекс.Вебмастер — «Страница из Sitemap запрещена к индексированию», и карта помечается как содержащая ошибки. Та же ошибка уже продублирована в собранной копии: docs/sitemap.xml:4-5 и docs/shop/sitemap.xml:4-5.
- **Починка:** Удалить строку 4 и строку 5 из /Users/nick/Desktop/Волосы/site/sitemap.xml (адреса privacy.html и terms.html) и строку 4 и строку 5 из /Users/nick/Desktop/Волосы/site-shop/sitemap.xml (terms.html и privacy.html). Ссылки на эти страницы в футере оставить как есть. Затем пересобрать docs: node tools/сборка-для-github.mjs https://nekitshit.github.io/hair-id

### 21. description и og:description каталога обещают бессульфатные и липидные средства, которых в каталоге нет

- **Где:** `site-shop/katalog.html:7` (направление: сео-техничка)
- **Как ломается:** Строка 7 и её копия в og:description на строке 13 обещают «Бессульфатные шампуни, липидные и протеиновые маски, уход за кожей головы, термозащита». По products.data.js (36 товаров): «сульфат» — 0 совпадений, «липидн» — 0. Человек, кликнувший в выдаче на «бессульфатный шампунь» или «липидная маска», попадает на витрину из шампуней, масел и кремов MOCHEQI MUSK/OLORCHEE и уходит. То же обещание уходит в превью ссылки в мессенджерах, потому что продублировано в og:description.
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/katalog.html строку 7 и строку 13 заменить на одно и то же значение: content="36 средств для волос от мастера Hair ID: шампуни, маски, масла, сыворотки, термозащита. Подбор по задаче волос, а не по бренду. СПб и Ленобласть." (145 знаков). Число 36 держать синхронным с длиной массива в products.data.js. Слово «бессульфатный» убрать и из keywords (строка 8), и из видимого лида на строке 88.

### 22. Шторка .page-veil прячет весь экран, пока не выполнится motion.js

- **Где:** `site/css/motion.css:136` (направление: производительность)
- **Как ломается:** Человек на мобильном интернете открывает главную. Класс has-motion ставится встроенным скриптом до первой отрисовки (site/index.html:36-42), правило motion.css:136-145 рисует непрозрачный fixed-слой цвета фона на весь вьюпорт (элемент — site/index.html:63). Снимает его только класс body.has-landed (motion.css:146), который ставится из CONFIG.loadStages (site/js/motion.js:40) через 100 мс после init(), а init() висит на DOMContentLoaded (site/js/motion.js:606-610), то есть после скачивания и выполнения analytics.js и motion.js. До этого момента экран однотонный. Аварийный выход один — снятие has-motion через 3000 мс (site/index.html:42).
- **Починка:** В /Users/nick/Desktop/Волосы/site/css/motion.css внутри @media (prefers-reduced-motion: no-preference): (1) в правило `.has-motion .page-veil` (строки 136-145) добавить строку `animation: page-veil-out 0.8s var(--ease-out-soft) 1.2s forwards;`; (2) сразу после этого правила добавить `@keyframes page-veil-out { to { opacity: 0; } }`; (3) строку 146 `.has-motion body.has-landed .page-veil { opacity: 0; }` заменить на `.has-motion body.has-landed .page-veil { animation: none; opacity: 0; }` — иначе после снятия шторки скриптом отложенная анимация вернёт её обратно. Тогда шторка уходит сама на 1,2 с даже без JS, а когда motion.js успевает раньше — работает прежний плавный сценарий. Ту же правку продублировать в /Users/nick/Desktop/Волосы/site-shop/css/motion.css и в обеих копиях в docs/ (docs/css/motion.css, docs/shop/css/motion.css).

### 23. Шрифты Google подключены через @import внутри CSS, preconnect отсутствует на 12 страницах из 16

- **Где:** `site/css/tokens.css:33` (направление: производительность)
- **Как ломается:** Человек открывает site/uslugi.html с телефона. tokens.css (28 452 байта) грузится, парсится, и только тогда браузер узнаёт адрес CSS шрифтов, дальше третьим запросом идут woff2 — три последовательных обращения вместо двух параллельных. На uslugi.html к этому добавляется полный DNS+TLS-хендшейк к fonts.googleapis.com и fonts.gstatic.com, потому что preconnect там нет вовсе. Пока цепочка идёт, страница закрыта шторкой .page-veil (находка 1), и текста не видно.
- **Починка:** Шаг 1: удалить строку 33 в /Users/nick/Desktop/Волосы/site/css/tokens.css и строку 33 в /Users/nick/Desktop/Волосы/site-shop/css/tokens.css (строка `@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;1,400&family=Manrope:wght@300;400;500;600;700&display=swap');`). Шаг 2: в <head> каждой страницы, которая грузит tokens.css, непосредственно ПЕРЕД строкой `<link rel="stylesheet" href="css/tokens.css">` вставить три строки: `<link rel="preconnect" href="https://fonts.googleapis.com">`, `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`, `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,500;0,600;1,400&family=Manrope:wght@300;400;500;600;700&display=swap">`. На site/index.html и site-shop/index.html две строки preconnect уже есть (site/index.html:26-27, site-shop/index.html:25-26) — там добавить только третью строку. Страницы, где нужны все три: site/kak-doehat.html, site/o-mastere.html, site/privacy.html, site/raboty.html, site/terms.html, site/uslugi.html, site-shop/kak-vybrat.html, site-shop/katalog.html, site-shop/nabory.html, site-shop/o-mastere.html, site-shop/privacy.html, site-shop/terms.html. Файлы site/card.html и site/cheatsheet.html не трогать — там уже стоит прямой <link> на шрифты. После правки повторить в docs/ (docs — собранная копия).

### 24. Логотип в подвале — PNG 288 КиБ размером 560×560 при показе шириной 96 px, без loading="lazy", на 10 страницах

- **Где:** `site/index.html:1212` (направление: производительность)
- **Как ломается:** Человек открывает site/kak-doehat.html с телефона. Жадных картинок на странице ровно две: монограмма в шапке (106 КиБ) и знак в подвале (288 КиБ). Знак стоит в самом низу страницы, увидят его единицы, но он качается сразу и отбирает канал у первого экрана и у motion.js, который снимает шторку. 288 КиБ из 394 КиБ всей жадной загрузки уходят на картинку, которая рисуется квадратом 96 px.
- **Починка:** В каждом из 10 файлов заменить строку `<img width="560" height="560" src="assets/logo/hair-id-mark.png" alt="Hair ID">` на `<img width="192" height="192" loading="lazy" decoding="async" src="assets/logo/hair-id-mark.png" alt="Hair ID">` (в магазине alt остаётся `Hair ID Shop`). Адреса: site/index.html:1212, site/raboty.html:242, site/kak-doehat.html:141, site/o-mastere.html:145, site/uslugi.html:294, site-shop/index.html:311, site-shop/katalog.html:109, site-shop/nabory.html:108, site-shop/kak-vybrat.html:177, site-shop/o-mastere.html:133. Пропорция 1:1 сохраняется, раскладка не сдвинется: site/css/main.css:1387-1388 задаёт width:96px; height:auto. Отдельно пережать сам файл assets/logo/hair-id-mark.png до 192×192 (сейчас 560×560, 295 098 байт). После правки повторить в docs/.

### 25. Во всём проекте нет ни одного srcset/sizes/<picture>: телефону отдаются картинки шириной 1200-1600 px

- **Где:** `site/index.html:113` (направление: производительность)
- **Как ломается:** Человек с экраном 360 CSS-px открывает главную. Строка 113 отдаёт hero-texture.jpg 1600×960, 163 КиБ, а правило site/css/main.css:208-215 растягивает её на ширину блока с opacity:0.5 под двумя затемняющими градиентами — при DPR 2 нужно 720 px, приходит 1600 px. Строка 202 отдаёт process-3.jpg 1200×1500, 191 КиБ, в одноколоночную вёрстку шириной около 330 px. Восемь фоновых фактур, которые подставляет site/js/texture.js:76-95, все шириной 1400 px, вместе 567 КиБ.
- **Починка:** Сгенерировать для каждой фотографии по 2-3 ширины (480/960/1600) в WebP и проставить srcset+sizes. Минимум, который окупается сразу и правится в одну строку: site/index.html:113 — `<img width="1600" height="960" src="assets/photo/hero-texture.jpg" srcset="assets/photo/hero-texture-800.jpg 800w, assets/photo/hero-texture.jpg 1600w" sizes="100vw" alt="" data-parallax="0.06">` (после того как файл hero-texture-800.jpg будет создан). Для фактур: в /Users/nick/Desktop/Волосы/site/js/texture.js в initTextures, где сейчас `img.src = 'assets/photo/' + file;` (строка 84), подставлять узкий вариант по `window.matchMedia('(max-width: 700px)').matches`. То же в site-shop/js/texture.js. После правки повторить в docs/.

### 26. Две крупные картинки глубоко ниже первого экрана грузятся без loading="lazy" — 310 КиБ впустую

- **Где:** `site/index.html:202` (направление: производительность)
- **Как ломается:** Человек открывает главную с телефона и остаётся на первом экране. Строка 202 (process-3.jpg, 191 КиБ) лежит в разделе «Почему наугад не работает», её родитель <figure> помечен data-reveal="clip", то есть до прокрутки она вообще спрятана. Строка 550 (texture-wide.jpg, 119 КиБ) — фоторазделитель в седьмом разделе, примерно середина страницы. Обе качаются одновременно с первым экраном и с motion.js, который снимает шторку.
- **Починка:** В /Users/nick/Desktop/Волосы/site/index.html строку 202 заменить на `<img width="1200" height="1500" loading="lazy" decoding="async" src="assets/photo/process-3.jpg" alt="Мастер расчёсывает волосы клиентки" data-parallax="0.08">`, строку 550 — на `<img width="1600" height="720" loading="lazy" decoding="async" src="assets/photo/texture-wide.jpg" alt="" data-parallax="0.16">`. Параллакс не пострадает: элементы остаются в DOM, motion.js собирает их по атрибуту, а не по факту загрузки. То же повторить в docs/index.html.

### 27. docs/ — устаревшая сборка: в опубликованной главной нет разметки перелетающего логотипа

- **Где:** `docs/index.html:54` (направление: html-структура)
- **Как ломается:** docs/ — это то, что отдаётся с GitHub Pages (ВЫКЛАДКА-НА-GITHUB.md, шаг 3: Branch main, папка /docs). В site/index.html:55-61 лежит пара <img class="brandmark brandmark--sign"> / <img class="brandmark brandmark--word">, в site/index.html:77-78 — header-logo__sign и header-logo__word. В docs/index.html на том же месте (строки 54-70) этих элементов нет вовсе, в шапке одна картинка без классов. Счётчики: brandmark — 2 в site/index.html и 0 в docs/index.html; 1 в site/css/main.css и 0 в docs/css/main.css; 2 в site/js/motion.js и 0 в docs/js/motion.js. hero__logo-slot — 2 и 3 в site, 0 и 0 в docs. diff site/css/main.css docs/css/main.css даёт 154 строки различий, включая целый блок .site-header::before, которого в сборке нет. Даты: docs собран 9 сентября 20:23, site/index.html изменён 9 сентября 20:33, site/css/main.css — 10 сентября 14:03. Проверил нормализованным diff: docs/uslugi.html отличается от site/uslugi.html только переписанным адресом магазина (это работа сборщика), то есть остальные страницы актуальны, а главная и CSS — нет.
- **Починка:** Из /Users/nick/Desktop/Волосы выполнить: node tools/сборка-для-github.mjs https://nekitshit.github.io/hair-id — скрипт пересоберёт docs/ из site/ и site-shop/ и перепишет адреса. После сборки убедиться, что docs/index.html содержит brandmark--sign и brandmark--word, docs/css/main.css — hero__logo-slot, а docs/js/motion.js — brandmark.

### 28. Длительность трёх процедур на главной и на странице услуг названа по-разному

- **Где:** `site/uslugi.html:161` (направление: html-структура)
- **Как ломается:** Кератиновое выпрямление: site/index.html:420 в карточке «2,5–4 часа» и site/index.html:732 в сравнительной таблице «2,5–4 часа», а site/uslugi.html:161 — «2–3 часа». Ботокс для волос: site/index.html:434 и site/index.html:733 — «2–3 часа», site/uslugi.html:175 — «1,5–2 часа». Холодное восстановление: site/index.html:734 (третья колонка таблицы, заголовок «Холодное восстановление» на index.html:690) — «1–1,5 часа», site/uslugi.html:133 — «1,5–2 часа». Экран: человек читает на главной «кератин 2,5–4 часа», нажимает «Услуги и цены» в шапке и на той же услуге видит «2–3 часа» — расхождение до полутора часов на самой длинной процедуре, при том что на главной же (index.html:1108) обещано «процедуры идут от полутора до четырёх часов».
- **Починка:** В /Users/nick/Desktop/Волосы/site/uslugi.html заменить: строка 161 <dd>2–3 часа</dd> → <dd>2,5–4 часа</dd> (кератин); строка 175 <dd>1,5–2 часа</dd> → <dd>2–3 часа</dd> (ботокс); строка 133 <dd>1,5–2 часа</dd> → <dd>1–1,5 часа</dd> (холодное восстановление). Набор с главной выбран потому, что он согласован и с карточками, и со сравнительной таблицей, и с фразой про «полтора-четыре часа». После правки пересобрать docs/.

### 29. На восьми страницах из десяти под футером на телефоне остаётся 192px пустоты — отступ под панель, которой нет

- **Где:** `site/css/main.css:1474` (направление: html-структура)
- **Как ломается:** site/css/main.css:1474 (внутри @media max-width:768px, открытого на строке 1449) задаёт .site-footer { padding-bottom: calc(var(--space-9) + 96px) }; при --space-9: 6rem (site/css/tokens.css:184) это 96+96=192px. Отступ нужен под фиксированную .mobile-bar (main.css:1341-1355, position:fixed, bottom:0, по умолчанию display:none, включается строкой 1473). Но разметка .mobile-bar есть только в site/index.html:1261-1264 — grep по site/*.html других вхождений не даёт: ни в uslugi.html, ни в raboty.html, ни в o-mastere.html, ни в kak-doehat.html, ни в privacy.html, ни в terms.html. То же в магазине: site-shop/css/shop.css:856 даёт тот же отступ (@media max-width:768px открыт на 852), а разметка есть только в site-shop/index.html:379-382 — в katalog.html, nabory.html, kak-vybrat.html, o-mastere.html её нет. Экран: телефон, любая внутренняя страница любого из двух сайтов — тёмный футер заканчивается пустой полосой почти в два пальца высотой, и при этом нет липкой кнопки «Записаться» / «Каталог», ради которой отступ и сделан.
- **Починка:** Если решение — оставить панель только на главных, то в /Users/nick/Desktop/Волосы/site/css/main.css строку 1474 заменить с «  .site-footer { padding-bottom: calc(var(--space-9) + 96px); }» на «  body:has(.mobile-bar) .site-footer { padding-bottom: calc(var(--space-9) + 96px); }», и точно так же в /Users/nick/Desktop/Волосы/site-shop/css/shop.css строку 856. Если решение — вернуть панель, то скопировать блок site/index.html:1261-1264 в uslugi.html, raboty.html, o-mastere.html, kak-doehat.html (ссылки tel:+79944269944 и index.html#booking) и блок site-shop/index.html:379-382 в katalog.html, nabory.html, kak-vybrat.html, o-mastere.html (ссылки katalog.html и nabory.html). После правки пересобрать docs/.

### 30. В четырёх опубликованных юридических документах остались незаполненные заглушки в квадратных скобках

- **Где:** `site/privacy.html:34` (направление: html-структура)
- **Как ломается:** site/privacy.html:34-35 «Оператором персональных данных является Давыдова Ирина, [статус: самозанятая / ИП], ИНН [инн]. Связаться можно по телефону 8 994 426-99-44 или написать на [почта]»; site/privacy.html:101 — редакторская заметка «[Если счётчик решено не подключать — этот раздел заменить…]»; site/terms.html:48 «[статус: самозанятая / ИП], ИНН [инн]»; site-shop/privacy.html:35-36 то же плюс инструкция на строке 85 «[Если подключён счётчик посещаемости — назвать его здесь…]»; site-shop/terms.html:32 «[статус: самозанятая / ИП], ИНН [инн]» и site-shop/terms.html:62 «Чек выдаётся [указать: в приложении «Мой налог» / через кассу]». Те же заглушки лежат в собранной копии: docs/privacy.html:34-35, docs/terms.html:48, docs/shop/privacy.html:35-36, docs/shop/terms.html:32. Ссылки на документы стоят в футере каждой страницы (site/index.html:1242-1243). Экран: посетитель открывает «Политику конфиденциальности» из футера и видит квадратные скобки вместо ИНН и адреса почты, а ниже — черновую заметку редактора прямо в тексте документа.
- **Починка:** Заполнить в четырёх файлах: /Users/nick/Desktop/Волосы/site/privacy.html строки 34-35, /Users/nick/Desktop/Волосы/site/terms.html строка 48, /Users/nick/Desktop/Волосы/site-shop/privacy.html строки 35-36, /Users/nick/Desktop/Волосы/site-shop/terms.html строка 32 — вместо «[статус: самозанятая / ИП]» подставить фактический статус, вместо «ИНН [инн]» — настоящий ИНН, вместо «[почта]» — рабочий адрес почты. Убрать редакторские заметки: site/privacy.html строки 101-102 и site-shop/privacy.html строки 85-87 удалить целиком либо заменить готовой фразой. В site-shop/terms.html строке 62 «[указать: в приложении «Мой налог» / через кассу]» заменить одним выбранным вариантом. Значения (статус, ИНН, почта) должен назвать владелец. После правки пересобрать docs/.

### 31. Политика конфиденциальности утверждает, что установлена Яндекс Метрика, а счётчик выключен

- **Где:** `site/privacy.html:96` (направление: html-структура)
- **Как ломается:** site/privacy.html:96-100 утвердительно: «На сайте установлен счётчик посещаемости Яндекс Метрика. Он собирает обезличенные сведения…». В site/js/analytics.js:25 стоит var YM_ID = 0, строка 28 var enabled = YM_ID > 0, и весь блок подключения на строке 34 обёрнут в if (enabled) — запрос к mc.yandex.ru не уходит никогда. В самом файле analytics.js (строки 6-13) прямо написано: «Пока номер равен нулю, счётчик не подключается». Рядом, site/privacy.html:101-102, лежит невыполненная заметка «[Если счётчик решено не подключать — этот раздел заменить на строку «Сайт не использует файлы cookie для отслеживания»]». При этом site-shop/privacy.html:83-84 по тому же поводу говорит противоположное: «Сайт не подключает рекламные системы… Браузер сохраняет служебные данные, необходимые для работы страницы». Экран: посетитель читает политику студии и получает описание обработки данных, которой нет, а открыв политику магазина — противоположное утверждение от того же оператора.
- **Починка:** Выбрать одно из двух. Вариант А (счётчик нужен): вписать номер в /Users/nick/Desktop/Волосы/site/js/analytics.js строку 25 вместо var YM_ID = 0; и удалить заметку site/privacy.html строки 101-102. Вариант Б (счётчика не будет): в /Users/nick/Desktop/Волосы/site/privacy.html заменить абзац строк 96-100 на «Сайт не использует файлы cookie для отслеживания.» и удалить строки 101-102 с заметкой; формулировку раздела 9 привести к тому же смыслу, что в site-shop/privacy.html:83-84. После правки пересобрать docs/.

### 32. В магазине список .about__facts остаётся без стилей — правила есть только в main.css сайта студии

- **Где:** `site-shop/o-mastere.html:115` (направление: html-структура)
- **Как ломается:** site-shop/o-mastere.html:115 — <ul class="about__facts" data-reveal-group> с тремя <li><strong>…</strong> текст</li> (строки 116-118). Правила .about__facts, .about__facts li, .about__facts strong объявлены только в /Users/nick/Desktop/Волосы/site/css/main.css:1042-1063. Магазин подключает вместо main.css файлы tokens.css, motion.css, shop.css, texture.css, shape.css, pages.css (site-shop/o-mastere.html:34-39) — grep по всей папке site-shop/css/ по слову about__facts не даёт ни одного совпадения. Общего сброса для ul в магазине тоже нет: в site-shop/css/tokens.css базовые стили (строки 394-450) задают box-sizing, body, h1-h4, p, a, img — ul среди них нет; в pages.css есть только .about-page__text > * + * (строка 156), маркеры и отступ он не трогает. Экран: телефон или десктоп, страница магазина «Кто собирает» — блок из трёх фактов рисуется браузерным умолчанием: маркеры-точки, отступ слева 40px, <strong> строчный. На site/o-mastere.html тот же блок — колонка без маркеров, с золотой линией слева и заголовком факта отдельной строкой.
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/css/pages.css после строки 156 (.about-page__text > * + * { margin-top: var(--space-4); }) добавить блок, скопированный из /Users/nick/Desktop/Волосы/site/css/main.css строк 1042-1063: .about__facts { margin: var(--space-6) 0 0; padding: 0; list-style: none; display: flex; flex-direction: column; gap: var(--space-4); } .about__facts li { padding-left: var(--space-5); border-left: 1px solid var(--c-border-strong); font-size: var(--fs-small); color: var(--c-text-muted); } .about__facts strong { display: block; color: var(--c-text); font-weight: var(--fw-semibold); margin-bottom: 2px; } — все использованные переменные объявлены в общем tokens.css. После правки пересобрать docs/.

### 33. Корзина живёт только в памяти — теряется при переходе на другую страницу и при перезагрузке

- **Где:** `site-shop/js/catalog.js:237` (направление: магазин)
- **Как ломается:** `var cart = {}` (строка 237), и во всём магазине нет ни одного обращения к хранилищу: grep -rn 'localStorage|sessionStorage' по site-shop/js, site-shop/css и всем html даёт ноль совпадений (единственные попадания на 'is-open' — это body.menu-is-open бургера). При этом кнопка «Заказ N» (id=cart-toggle) и панель id=cart-panel продублированы на всех пяти страницах (index.html:88/363, katalog.html:69/158, nabory.html:69/157, o-mastere.html:69/182, kak-vybrat.html:69/226), и catalog.js подключён на всех пяти. Сценарий: человек на katalog.html набирает 4 средства, жмёт в шапке «Наборы» → nabory.html, счётчик 0, панель не показывается (renderCart: panel.classList.toggle('is-visible', n > 0)), заказ пуст. То же после F5 и после «назад».
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/js/catalog.js сразу после строки 237 (`var cart = {};`) добавить:

  var CART_KEY = 'hairid-shop-cart';
  function saveCart() {
    try { localStorage.setItem(CART_KEY, JSON.stringify(cart)); } catch (e) {}
  }
  function loadCart() {
    try {
      var raw = JSON.parse(localStorage.getItem(CART_KEY) || '{}');
      Object.keys(raw).forEach(function (id) {
        if (lineById(id) && raw[id] > 0) cart[id] = raw[id];
      });
    } catch (e) {}
  }

В функции add() (строки 455-459) после `if (cart[id] < 1) delete cart[id];` дописать `saveCart();`.
В init() (строка 528) перед первым renderProducts() вызвать `loadCart();` — обязательно после того, как PRODUCTS уже переопределён живыми данными (это происходит на строках 262-278, то есть до init).
Пересобрать docs: node tools/сборка-для-github.mjs.

### 34. Клик по любому месту карточки товара добавляет его в заказ

- **Где:** `site-shop/css/shape.css:298` (направление: магазин)
- **Как ломается:** Строки 295-305: `[data-card-link] .card-stretch::after, … [data-card-link] .product__add::after, [data-card-link] .set__add::after { content:''; position:absolute; inset:0; z-index:1; }`, а строки 310-316 задают этим же элементам `position: static`. Ближайший позиционированный предок — сама карточка: `[data-card-link] { position: relative }` (строка 293), а `.product`, `.product__body`, `.product__foot` в shop.css (строки 338, 363, 428) позиционирования не имеют — проверил все три блока. Значит невидимый слой кнопки «В заказ» растягивается на всю карточку. Из-под него выведены только details/summary/прочие кнопки (строки 319-327, причём .product__add и .set__add из этого списка явно исключены). Клик по фотографии, заголовку или строке «Кому:» браузер отдаёт кнопке (клик по псевдоэлементу адресуется его владельцу), обработчик на catalog.js:477 находит [data-add] и вызывает add(). Сценарий на телефоне: человек тыкает в фото банки, чтобы рассмотреть, — товар молча падает в заказ. То же у наборов: .set__add, строки 299 и 314, карточка `<article class="set" … data-card-link>` (catalog.js:358).
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/css/shape.css:
1) удалить строки 298 и 299 (`[data-card-link] .product__add::after,` и `[data-card-link] .set__add::after`), оставив в селекторе на строке 297 `[data-card-link] .service-card__cta::after {`;
2) удалить строки 313 и 314 (`[data-card-link] .product__add,` и `[data-card-link] .set__add`), оставив `[data-card-link] .service-card__cta {` на строке 312;
3) на строке 320 убрать исключения кнопок: `[data-card-link] button:not(.service-card__cta),` вместо `[data-card-link] button:not(.service-card__cta):not(.product__add):not(.set__add),` — тогда кнопки заказа гарантированно остаются нажимаемыми поверх любого другого слоя.
Пересобрать docs: node tools/сборка-для-github.mjs (docs/shop/css/shape.css сейчас идентичен).

### 35. Кнопка «Заказ N» в шапке ничего не делает — класса is-open в CSS нет

- **Где:** `site-shop/js/catalog.js:491` (направление: магазин)
- **Как ломается:** Строка 491: `document.getElementById('cart-panel').classList.toggle('is-open')`. В CSS панель показывается единственным правилом `.cart-panel.is-visible { transform: translateY(0); }` — shop.css строка 666, базовое состояние `transform: translateY(140%)` на строке 663. Селектора `.cart-panel.is-open` нет ни в одном css-файле магазина: grep 'is-open' по css/ даёт только body.menu-is-open (shop.css:868-887, бургер-меню). Сценарий: на katalog.html человек добавил 3 позиции, свернул панель крестиком (shop-ui.js:51 снимает is-visible), жмёт «Заказ 3» в шапке — не происходит ничего; вернуть панель можно только добавив ещё товар (renderCart строка 401 сама выставит is-visible). На o-mastere.html и kak-vybrat.html кнопка мертва всегда — там товаров нет.
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/js/catalog.js строку 491 заменить с
  document.getElementById('cart-panel').classList.toggle('is-open');
на
  var p = document.getElementById('cart-panel');
  p.classList.remove('is-dismissed');
  p.classList.toggle('is-visible');
(remove('is-dismissed') нужен, потому что shop-ui.js:53 вешает этот класс через 600 мс после закрытия крестиком.)
Пересобрать docs: node tools/сборка-для-github.mjs.

### 36. У 16 товаров в блоке «Кому:» стоит служебная заглушка вида «Шампунь OLORCHEE»

- **Где:** `site-shop/js/products.data.js:442` (направление: магазин)
- **Как ломается:** Генератор tools/товары-в-магазин2.mjs, функция кому(), строка 39: при пустом поле «польза» возвращает `п.категория + ' ' + п.бренд`. В данных это 16 позиций: строки 372, 389, 406, 424, 442, 460, 478, 496, 514, 532, 550, 568, 586, 604, 622, 640 («Шампунь CHINCHY», «Кондиционер CHINCHY», «Шампунь OLORCHEE» и т. д.). Карточка выводит это после жирной подписи «Кому:» — catalog.js:337. На katalog.html подряд идут четыре шампуня OLORCHEE (t028-t031, строки 442, 460, 478, 496), и у всех четырёх главный продающий текст карточки — одно и то же «Кому: Шампунь OLORCHEE».
- **Починка:** Правильный путь: дописать «польза» для этих 16 позиций в поставщик/позиции.json и выполнить node tools/товары-в-магазин2.mjs.
Страховка от повторения — в /Users/nick/Desktop/Волосы/tools/товары-в-магазин2.mjs строку 39 заменить на:
  return п1 ? п1.replace(/^./, c => c.toUpperCase()).replace(/\.$/, '') : '';
и в /Users/nick/Desktop/Волосы/site-shop/js/catalog.js строку 337 заменить на:
  (p.forWhom ? '<p class="product__for"><b>Кому:</b> ' + p.forWhom + '</p>' : '') +
Потом пересобрать данные и docs.

### 37. Названия пяти товаров обрезаны многоточием посреди слова

- **Где:** `site-shop/js/products.data.js:77` (направление: магазин)
- **Как ломается:** Строки 77 («Крем-шампунь с кератином и маслом жожоба для чувствительной…»), 165 («…и экстрактом зародышей…»), 255 («…для сильно повреждён…»), 540 («…и экстрактом под…»), 558 («…с гиалуроновой к…»). Обрезка в tools/товары-в-магазин2.mjs:62: `п.название.length > 62 ? п.название.slice(0, 59).trim() + '…' : п.название` — без учёта границы слова. Полные названия лежат в товары-цены.txt (t005 «…для чувствительной кожи головы», t010 «…экстрактом зародышей риса», t016 «…для сильно повреждённых волос»). Сценарий: в каталоге (catalog.js:333, `<h3>`) и в списке состава набора (catalog.js:365) заголовок обрывается на середине слова — покупателю не видно, для чего средство.
- **Починка:** В /Users/nick/Desktop/Волосы/tools/товары-в-магазин2.mjs строку 62 заменить с
  title: п.название.length > 62 ? п.название.slice(0, 59).trim() + '…' : п.название,
на
  title: п.название.length > 62 ? п.название.slice(0, 59).replace(/\s+\S*$/, '').trim() + '…' : п.название,
(или убрать обрезку совсем — заголовок карточки многострочный, .product h3 в shop.css:371 ширину не ограничивает). Затем node tools/товары-в-магазин2.mjs и пересборка docs.

### 38. Обещание «у каждого средства написано, кому оно не подходит» не выполняется ни для одного товара

- **Где:** `site-shop/o-mastere.html:113` (направление: магазин)
- **Как ломается:** grep -c '"notFor": "—"' по products.data.js даёт 36 из 36 — поле жёстко проставляется генератором (tools/товары-в-магазин2.mjs:69, `notFor: '—'`). Карточка выводит блок только при условии `p.notFor !== '—'` (catalog.js:338), то есть он не показывается никогда. При этом o-mastere.html:112-113 обещает «здесь у каждого средства написано не только кому оно подходит, но и кому не подходит», terms.html:84-85 ссылается на это как на причину отказа в возврате («Именно поэтому на каждой карточке написано, кому средство не подойдёт»), index.html:122 — «объяснено: кому подходит, кому нет и почему», плюс index.html:14 то же самое в og:description для соцсетей. Сценарий: покупатель открывает любую карточку, ищет обещанное предупреждение — его нет; в споре о возврате ссылка из terms.html повисает в воздухе.
- **Починка:** Быстрый честный вариант — снять обещание до заполнения данных:
— /Users/nick/Desktop/Волосы/site-shop/o-mastere.html, строки 112-113: убрать «не только кому оно подходит, но и кому не подходит», оставив «здесь у каждого средства написано, кому оно подходит».
— /Users/nick/Desktop/Волосы/site-shop/terms.html, строки 84-85: убрать оборот «на каждой карточке написано, кому средство не подойдёт», оставить ссылку на бесплатный подбор по фотографии.
— /Users/nick/Desktop/Волосы/site-shop/index.html, строка 122 и строка 14 (og:description): «объяснено: кому подходит и почему».
Содержательный вариант — заполнить notFor в поставщик/позиции.json хотя бы у ходовых позиций и убрать заглушку `notFor: '—'` в tools/товары-в-магазин2.mjs:69, затем пересобрать данные. После любой правки — node tools/сборка-для-github.mjs.

### 39. Обещана «экономия от 190 до 380 ₽» в наборах, а у всех наборов цены нет вовсе

- **Где:** `site-shop/index.html:177` (направление: магазин)
- **Как ломается:** У всех четырёх наборов в catalog.js price и old равны null: строки 193-194 (set-after-keratin), 202-203 (set-dry), 211-212 (set-scalp), 220-221 (set-event). renderSets (строка 367) печатает `money(s.price)` → «Цена по запросу», а перечёркнутая старая цена не выводится вовсе, потому что s.old пустой. Текст же обещает конкретную выгоду: index.html:177 «В наборе экономия от 190 до 380 ₽», nabory.html:88 то же, плюс nabory.html:7 (meta description) и nabory.html:13 (og:description). Сценарий: человек приходит из поиска по описанию «Экономия от 190 до 380 ₽», открывает «Наборы» и видит четыре карточки «Цена по запросу» — ни цены, ни старой цены, ни экономии.
- **Починка:** Либо проставить реальные price и old у четырёх наборов в /Users/nick/Desktop/Волосы/site-shop/js/catalog.js (строки 193-194, 202-203, 211-212, 220-221) после того, как владелец заполнит товары-цены.txt, либо убрать цифру:
— index.html:177 → «Набор дешевле, чем те же позиции по отдельности; сумму подтвержу в ответе.»
— nabory.html:88 → та же формулировка;
— nabory.html:7 и nabory.html:13 → убрать «Экономия от 190 до 380 ₽.» из description и og:description.
Пересобрать docs: node tools/сборка-для-github.mjs.

### 40. На первом экране написано «12 отобранных средств», каталог ниже показывает 36

- **Где:** `site-shop/index.html:131` (направление: магазин)
- **Как ломается:** Строка 131: `<li><strong>12</strong> <span>отобранных средств</span></li>`. Живых товаров 36 (grep -c '"id":' по products.data.js = 36), и счётчик #shop-count на этой же странице (index.html:161, заполняется в catalog.js:315-321 значением list.length при activeTask='all') выведет «36 средств». Сценарий: человек видит «12» на первом экране, прокручивает на один экран вниз к каталогу и видит «36 средств» — два разных числа на одной странице. Рядом kak-vybrat.html:7 и o-mastere.html:116 держат ту же линию про «мало позиций».
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/index.html строку 131 заменить на `<li><strong id="shop-count-hero">36</strong> <span>отобранных средств</span></li>`, а в /Users/nick/Desktop/Волосы/site-shop/js/catalog.js в renderProducts после блока со счётчиком (после строки 321) добавить:
  var hero = document.getElementById('shop-count-hero');
  if (hero) hero.textContent = PRODUCTS.length;
(именно PRODUCTS.length, а не list.length — иначе цифра будет меняться при фильтрации по задаче). Пересобрать docs.

### 41. Юридические страницы выложены с незаполненными скобками-заготовками

- **Где:** `site-shop/terms.html:32` (направление: магазин)
- **Как ломается:** terms.html строки 31-32: «Продавец: Давыдова Ирина, [статус: самозанятая / ИП], ИНН [инн], телефон 8 994 426-99-44»; terms.html:62 — «Чек выдаётся [указать: в приложении «Мой налог» / через кассу]»; privacy.html строки 35-36 — «Оператором персональных данных является Давыдова Ирина, [статус: самозанятая / ИП], ИНН [инн]. Связаться можно по телефону … или написать на [почта]»; privacy.html:85-87 — «[Если подключён счётчик посещаемости — назвать его здесь: …]». Сценарий: покупатель открывает «Условия заказа, оплаты, доставки и возврата» из подвала, чтобы понять, кому он платит, и вместо реквизитов видит квадратные скобки; в политике конфиденциальности нет адреса для обращения субъекта персональных данных.
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/terms.html: строка 32 — подставить статус и ИНН вместо «[статус: самозанятая / ИП], ИНН [инн]»; строка 62 — вместо «[указать: в приложении «Мой налог» / через кассу]» оставить один способ. В /Users/nick/Desktop/Волосы/site-shop/privacy.html: строки 35-36 — статус, ИНН и почта вместо «[почта]»; строки 85-87 — либо назвать счётчик, либо удалить абзац в скобках целиком. Данные должен дать владелец. После правки пересобрать docs (node tools/сборка-для-github.mjs) — в docs/shop лежат те же тексты.

### 42. Цель Метрики shop_click не сработает: ищется подстрока старого домена

- **Где:** `site/js/analytics.js:87` (направление: сборка-docs)
- **Как ломается:** Владелец вписывает номер счётчика вместо YM_ID = 0 (site/js/analytics.js:25). Посетитель на главной нажимает «Перейти в магазин ухода» (docs/index.html:874) — обработчик проверяет a.href.indexOf('hairid-shop'), а href после сборки равен https://nekitshit.github.io/hair-id/shop, подстроки 'hairid-shop' в нём нет. goal('shop_click') не вызывается, в Метрике переход студия→магазин навсегда нулевой.
- **Починка:** В /Users/nick/Desktop/Волосы/site/js/analytics.js строку 87 и в /Users/nick/Desktop/Волосы/site-shop/js/analytics.js строку 87 заменить
      if (a.href.indexOf('hairid-shop') !== -1) return goal('shop_click');
на
      if (a.href.indexOf('/shop') !== -1 && location.pathname.indexOf('/shop') === -1) return goal('shop_click');
Вторая половина условия обязательна: без неё в самом магазине (адрес /hair-id/shop/) цель срабатывала бы на каждой внутренней ссылке. После правки пересобрать docs/.

### 43. Внутренняя записка «это сток, фото мастера чужое» уедет на живой сайт

- **Где:** `docs/assets/photo/ИСТОЧНИКИ.md:14` (направление: сборка-docs)
- **Как ломается:** После включения Pages любой человек открывает https://nekitshit.github.io/hair-id/assets/photo/ИСТОЧНИКИ.md и читает: «⚠️ Важно: это временные фотографии», «Блок „Работы“ — заменить своими снимками в первую очередь», «Фото мастера — обязательно своё. Чужое лицо в разделе „О мастере“ разрушает доверие». При этом на сайте эти снимки ещё стоят: docs/o-mastere.html:95 и docs/index.html:880 отдают assets/photo/master.jpg с alt «Ирина Давыдова за работой», docs/index.html:353,366,559,565,577 — work-1/2/4.jpg в блоке «Работы».
- **Починка:** В /Users/nick/Desktop/Волосы/tools/сборка-для-github.mjs после строки 39 (const рас = path.extname(...)) вставить строку:
    if (рас === '.md') continue;   // внутренние записки на сайт не выкладываем
и пересобрать. Проверка: find docs -name '*.md' должно вернуть пусто (сейчас возвращает четыре файла).

### 44. .gitignore пропускает ~40 МБ фото Авито, graphify-out и отладочные снимки

- **Где:** `.gitignore:6` (направление: сборка-docs)
- **Как ломается:** Владелец выполняет git init && git add -A && git commit из ВЫКЛАДКА-НА-GITHUB.md:29. В коммит уходят: 108 jpg на 37 МБ из avito/ФОТО-И-ВИДЕО (строка 6 закрывает только 12 файлов видео.mp4), 14 jpg на 2.8 МБ из avito/фото-инфографика, вся папка graphify-out на 8.2 МБ (graph.html 1.9 МБ, graph.json 2.6 МБ), дамп .playwright-mcp с телефоном владельца и два отладочных снимка в корне на 1.5 МБ. Убрать их из публичного репозитория потом можно только переписыванием истории.
- **Починка:** Дописать в /Users/nick/Desktop/Волосы/.gitignore до первого коммита:
avito/ФОТО-И-ВИДЕО/
avito/фото-инфографика/
graphify-out/
.playwright-mcp/
header-1440-docked.png
шапка-1440-после-посадки.png
Строку 6 (avito/ФОТО-И-ВИДЕО/*/видео.mp4) после этого можно удалить — она перекрыта. Проверка перед коммитом: git add -A -n | wc -l и du -sh (сейчас 153 МБ на всю папку).

### 45. Папка поставщик/ с контактами фабрик и закупочной стратегией уедет в публичный репозиторий

- **Где:** `поставщик/ПОСТАВЩИКИ.md:91` (направление: сборка-docs)
- **Как ломается:** git add -A из ВЫКЛАДКА-НА-GITHUB.md:29 забирает все четыре файла папки поставщик/ (её нет в .gitignore), и после push они открыты по ссылке на github.com. В строке 91 — manager@chinchy.com и WhatsApp +86 139 2610 1130, в строке 139 — ek.krasota@gmail.com и +375 17 268-00-28, в таблице строк 103-110 — ещё восемь рабочих контактов фабрик. Строки 10-19 и 31-45 — разбор того, что MOCHEQI, CHINCHY и OLORCHEE суть маркетинговая легенда, и оценка рисков параллельного импорта с упоминанием ИП из Коломны, которое заявляет права на Россию и Беларусь. То есть закупочная схема владельца становится видна и конкурентам, и тем самым правообладателям.
- **Починка:** Дописать в /Users/nick/Desktop/Волосы/.gitignore до первого коммита две строки:
поставщик/
товары-цены.txt
Отдельно сказать владельцу вслух: GitHub Pages на бесплатном тарифе работает только из публичного репозитория, поэтому «сделать репозиторий приватным» — не вариант, эти файлы надо именно не коммитить. Инструкция ВЫКЛАДКА-НА-GITHUB.md:32 создаёт репозиторий, не оговаривая видимость.


## МЕЛОЧЬ

### 1. В параллаксе чтение геометрии чередуется с записью transform

- **Где:** `site/js/motion.js:516` (направление: js)
- **Как ломается:** frame() (строка 490) крутится бесконечным requestAnimationFrame (строка 522) и безусловно вызывает updateParallax() на строке 516. Внутри updateParallax (строки 269–284) в одном цикле сначала читается item.el.getBoundingClientRect(), сразу следом пишется item.el.style.transform — и так по каждому элементу. Пока страницу прокручивают, значения transform на каждом кадре разные, каждая запись помечает стили грязными, и следующий getBoundingClientRect заставляет браузер пересчитывать стили и раскладку заново — то есть примерно два десятка принудительных пересчётов за кадр (на site/index.html 9 элементов с data-parallax в разметке плюс те, что добавляет texture.js:93 и подхватывает HairIDMotion.refresh() из texture.js:357). Видно как подёргивание при прокрутке главной на телефоне.
- **Починка:** В файле /Users/nick/Desktop/Волосы/site/js/motion.js в updateParallax (строки 269–284) разнести проходы: сначала собрать все прямоугольники — `var rects = parallaxItems.map(function (item) { return item.el.getBoundingClientRect(); });`, затем вторым циклом по индексу писать transform, и перед записью сравнивать со строкой, записанной в прошлый раз, как уже сделано в updateLogoMorph на строке 450: `if (стиль !== item.записано) { item.записано = стиль; item.el.style.transform = стиль; }`.

### 2. Два поля формы записи размечены только placeholder-ом

- **Где:** `site/index.html:1170` (направление: доступность)
- **Как ломается:** У шага 4 `<label class="booking-form__label" for="bf-name">` (строка 1164) связан только с полем имени. `#bf-len` (строка 1170) и `#bf-note` (строка 1173) не имеют ни `<label for>`, ни `aria-label`, ни `aria-labelledby` — весь пояснительный текст живёт в `placeholder`. Воспроизводимая поломка: человек вводит первый символ в поле «Длина волос: до плеч / до лопаток / ниже» — placeholder исчезает, и поле шириной в колонку grid `1fr 1.4fr` (main.css:1212) остаётся без единой подписи. Вернувшись к форме после выбора дня и времени, он не знает, что в этом поле и в textarea от него хотели.
- **Починка:** В /Users/nick/Desktop/Волосы/site/index.html строка 1170: после `name="length"` добавить `aria-label="Длина волос"`. Строка 1173: после `name="note"` добавить `aria-label="Что беспокоит и что делали за последний год"`. Минимальный вариант без правки вёрстки; видимые `<label>` — если владелец согласится менять раскладку.

### 3. На uslugi.html нет ни одного h2: после h1 сразу h3

- **Где:** `site/uslugi.html:114` (направление: доступность)
- **Как ломается:** Проверил grep-ом все заголовки uslugi.html: единственный h1 на строке 86, дальше двенадцать карточек услуг с `<h3>` (114, 128, 142, 156, 170, 184, 198, 212, 226, 240, 254, 268) и три служебных h3 в футере (299, 311, 322). Уровня h2 на странице нет вообще. То же на kak-doehat.html (h1 на 86 → h3 на 96, 104, 112, 122) и на site-shop/katalog.html, nabory.html, o-mastere.html. Воспроизведение: в NVDA нажать «2» на uslugi.html — не находится ничего, хотя двенадцать карточек на странице есть; список заголовков показывает разрыв 1→3.
- **Починка:** В /Users/nick/Desktop/Волосы/site/uslugi.html в строках 114, 128, 142, 156, 170, 184, 198, 212, 226, 240, 254, 268 заменить `<h3>` на `<h2>` и `</h3>` на `</h2>`. Обязательно вместе с этим: в /Users/nick/Desktop/Волосы/site/css/pages.css строку 51 заменить `.usluga__top h3 { margin: 0; font-size: 1.18rem; line-height: 1.25; }` на `.usluga__top h2 { margin: 0; font-size: 1.18rem; line-height: 1.25; }` — иначе заголовки карточек потеряют размер и отступы. Ту же пару правок повторить в /Users/nick/Desktop/Волосы/docs/.

### 4. Превью сообщения объявляется заново после каждой нажатой буквы

- **Где:** `site/index.html:1177` (направление: доступность)
- **Как ломается:** `<div class="booking-preview" id="booking-preview" aria-live="polite">` (строка 1177) содержит `<pre id="booking-preview-text">` (строка 1179), в который site/js/booking.js:145 целиком переписывает собранное сообщение. Перерисовка привязана к `input` на форме — `form.addEventListener('input', readFields)` (booking.js:99), а readFields в конце вызывает render() (booking.js:108). Воспроизведение: со скринридером набрать в поле «Что беспокоит» длинную фразу — на каждой паузе в наборе живая область зачитывается заново, целиком, начиная с «Здравствуйте! Хочу записаться в Hair ID. Услуга: …», хотя изменился один символ.
- **Починка:** В /Users/nick/Desktop/Волосы/site/index.html строку 1177 заменить `<div class="booking-preview" id="booking-preview" aria-live="polite">` на `<div class="booking-preview" id="booking-preview">` — блок справочный, а не оповещательный, сам `<pre>` продолжит обновляться как раньше. Ту же правку повторить в /Users/nick/Desktop/Волосы/docs/index.html.

### 5. Фильтры каталога не сообщают ни своё состояние, ни результат

- **Где:** `site-shop/js/catalog.js:302` (направление: доступность)
- **Как ломается:** Чипы задач рисуются как `<button type="button" class="task-chip..." data-task="…">` без `aria-pressed` (catalog.js:302); активность выражена только классом `is-active` (стиль shop.css:312–318). Нажатие полностью перерисовывает `#shop-grid` и переписывает счётчик `<span id="shop-count">` (catalog.js:316–322), но счётчик лежит в обычном параграфе — katalog.html:95 `<p class="catalog__count"><span id="shop-count"></span></p>`, то же site-shop/index.html:161, ни `aria-live`, ни `role="status"` нет (grep по всей папке site-shop подтверждает: этих атрибутов у счётчика нет). Воспроизведение: со скринридером нажать чип «Жирные корни» на katalog.html — не будет ни «нажато», ни числа оставшихся средств; чтобы понять, изменилось ли что-то, придётся вручную уходить вниз и пересчитывать карточки. То же у фильтра галереи site/raboty.html:94–98 — site/js/pages.js:14–16 переключает только класс. При этом карточки подбора на главной сайта состояние объявляют правильно: picker.js:92 и 100 ставят `aria-pressed`.
- **Починка:** 1) В /Users/nick/Desktop/Волосы/site-shop/js/catalog.js строку 302 заменить на
```
      return '<button type="button" class="task-chip' + (t.key === activeTask ? ' is-active' : '') + '" aria-pressed="' + (t.key === activeTask ? 'true' : 'false') + '" data-task="' + t.key + '">' +
```
2) В /Users/nick/Desktop/Волосы/site-shop/katalog.html строку 95 и /Users/nick/Desktop/Волосы/site-shop/index.html строку 161 заменить на `      <p class="catalog__count" role="status"><span id="shop-count"></span></p>`.
3) В /Users/nick/Desktop/Волосы/site/js/pages.js строки 14–16 заменить
```
      панель.querySelectorAll('.works-filter__btn').forEach(function (b) {
        b.classList.toggle('is-active', b === кнопка);
      });
```
на
```
      панель.querySelectorAll('.works-filter__btn').forEach(function (b) {
        b.classList.toggle('is-active', b === кнопка);
        b.setAttribute('aria-pressed', b === кнопка ? 'true' : 'false');
      });
```
и в /Users/nick/Desktop/Волосы/site/raboty.html строкам 94–98 дописать `aria-pressed="false"` (у кнопки «Все» на строке 94 — `aria-pressed="true"`). Все правки повторить в собранной копии /Users/nick/Desktop/Волосы/docs/.

### 6. meta description главной 220 знаков — хвост с телефоном обрезается в выдаче

- **Где:** `site/index.html:7` (направление: сео-техничка)
- **Как ломается:** Замерил длину значения атрибута content на строке 7: 220 знаков при пределе показа около 150-160. В сниппете описание обрежется примерно на «…Студия в Осельках, Всеволожский район — приним…», и хвост «принимаю из Петербурга и Ленобласти. Запись: 8 994 426-99-44» не увидит никто. Телефон в description и не нужен: в сниппете он не кликабелен, а место занимает.
- **Починка:** В /Users/nick/Desktop/Волосы/site/index.html строку 7 заменить на: <meta name="description" content="Кератин без формальдегида, ботокс, восстановление после осветления, причёски. Диагностика по фото бесплатно. Студия в Осельках, Всеволожский район, Ленобласть."> — 159 знаков, регион сохранён, телефон убран (он остаётся в JSON-LD, строка 1280).

### 7. robots.txt из подпапки GitHub Pages не читается — карта магазина не имеет ни одного пути обнаружения

- **Где:** `docs/robots.txt:4` (направление: сео-техничка)
- **Как ломается:** По ВЫКЛАДКА-НА-GITHUB.md сайт публикуется на https://nekitshit.github.io/hair-id/. Робот запрашивает robots.txt только из корня хоста — https://nekitshit.github.io/robots.txt, это файл соседнего репозитория. Файлы docs/robots.txt и docs/shop/robots.txt лягут по адресам /hair-id/robots.txt и /hair-id/shop/robots.txt и прочитаны не будут, то есть директива Sitemap на docs/robots.txt:4 не сработает никогда. Для карты студии это терпимо (её можно отправить руками), а карта магазина docs/shop/sitemap.xml не упомянута больше нигде: в docs/sitemap.xml перечислены только семь адресов студии, ссылки на карту магазина в ней нет. В итоге четыре страницы магазина не имеют ни одного пути обнаружения через sitemap.
- **Починка:** Файлы robots.txt оставить (пригодятся на своём домене). В /Users/nick/Desktop/Волосы/site/sitemap.xml добавить перед </urlset> адреса магазина (https://hairid-shop.netlify.app/katalog.html, /nabory.html, /kak-vybrat.html, /o-mastere.html и корень) либо сделать из корневой карты sitemapindex со ссылкой на карту магазина, затем пересобрать docs командой node tools/сборка-для-github.mjs https://nekitshit.github.io/hair-id. Дополнительно в Search Console и Вебмастере добавить ресурс с префиксом https://nekitshit.github.io/hair-id/ и отправить обе карты руками.

### 8. og:image квадратный при twitter:card=summary_large_image — превью обрезается

- **Где:** `site/index.html:16` (направление: сео-техничка)
- **Как ломается:** На строке 20 объявлен <meta name="twitter:card" content="summary_large_image">, рассчитанный на картинку 2:1, а на строке 16 указан hair-id-badge-ru.jpg — проверил файл, 1254×1254, квадрат. Отправьте ссылку на сайт в X — карточка обрежется до горизонтальной полосы по центру: верх и низ бейджа с названием студии срежутся. Facebook по og:image кадрирует к 1.91:1 и срежет то же самое. Пары «твиттер-карточка + квадратная картинка» на всех десяти страницах с og:image: site — index:16/20, uslugi:16/20, raboty:16/20, o-mastere:16/20, kak-doehat:16/20; site-shop — index:15/19, katalog:14/18, nabory:14/18, kak-vybrat:14/18, o-mastere:14/18.
- **Починка:** Самая дешёвая правка: на всех десяти страницах заменить строку <meta name="twitter:card" content="summary_large_image"> на <meta name="twitter:card" content="summary"> — маленькая квадратная карточка отрисуется без обрезки. Правильнее: подготовить assets/logo/hair-id-og.jpg размером 1200×630 с логотипом по центру, прописать его во все og:image и добавить рядом <meta property="og:image:width" content="1200"> и <meta property="og:image:height" content="630">. После правки пересобрать docs.

### 9. title главной 65 знаков — географический хвост обрезается

- **Где:** `site/index.html:6` (направление: сео-техничка)
- **Как ломается:** Строка 6: «Восстановление волос, кератин, ботокс — Hair ID, СПб и Ленобласть» — посчитал, 65 знаков при пределе показа около 60 (у кириллицы буквы шире, поэтому обрезка наступает раньше). В выдаче хвост превратится в «…Hair ID, СПб и Лен…», то есть один из двух географических маркеров пропадёт именно там, где он и нужен — в заголовке сниппета.
- **Починка:** В /Users/nick/Desktop/Волосы/site/index.html строку 6 заменить на: <title>Кератин и восстановление волос — Hair ID, СПб и Ленобласть</title> — 58 знаков, оба региона целы, ключевые слова стоят в начале. Затем пересобрать docs.

### 10. Перекрёстные ссылки в собранной копии ведут на адрес без слэша и получают 301

- **Где:** `docs/index.html:79` (направление: сео-техничка)
- **Как ломается:** В docs все ссылки студия→магазин указывают на https://nekitshit.github.io/hair-id/shop без завершающего слэша: docs/index.html:79, :860, :1210, docs/uslugi.html:64 и :306, docs/raboty.html:64 и :254, docs/o-mastere.html:64 и :157, docs/kak-doehat.html:64 и :153. Обратные ссылки магазин→студия — на https://nekitshit.github.io/hair-id, тоже без слэша: docs/shop/index.html:63, :82, :294, :338 и по две на каждой из katalog, nabory, kak-vybrat, o-mastere. GitHub Pages на адрес каталога без слэша отвечает 301 на вариант со слэшем, а в картах записан канонический адрес со слэшем (docs/shop/sitemap.xml:3). Нажатие «Магазин ухода» в футере даёт лишний переход, и внутренние ссылки расходятся с картой сайта. В исходниках site/ и site-shop/ проблемы нет: там ссылка вида https://hairid-shop.netlify.app — голый домен без пути, браузер добавляет корневой слэш сам, редиректа не возникает; путь /shop появляется только при сборке.
- **Починка:** Дописать завершающий слэш в исходниках — тогда сборка перенесёт его сама, потому что tools/сборка-для-github.mjs (строки 60-70) меняет только префикс без слэша. В site/index.html, uslugi.html, raboty.html, o-mastere.html, kak-doehat.html заменить href="https://hairid-shop.netlify.app" на href="https://hairid-shop.netlify.app/"; в site-shop/index.html, katalog.html, nabory.html, kak-vybrat.html, o-mastere.html заменить href="https://hairid.netlify.app" на href="https://hairid.netlify.app/". Затем пересобрать docs: node tools/сборка-для-github.mjs https://nekitshit.github.io/hair-id

### 11. Favicon — PNG 504×504 весом 136 КиБ

- **Где:** `site/index.html:23` (направление: производительность)
- **Как ломается:** Первый заход на сайт с телефона: браузер тянет assets/logo/hair-id-icon.png на 139 410 байт, чтобы показать иконку размером 16-32 px во вкладке. Тот же файл указан и как apple-touch-icon (строка 24).
- **Починка:** Сделать два файла: /Users/nick/Desktop/Волосы/site/assets/logo/favicon-32.png (32×32) и apple-touch-icon-180.png (180×180) из существующего hair-id-icon.png. Затем в 10 файлах заменить пару строк `<link rel="icon" type="image/png" href="assets/logo/hair-id-icon.png">` и `<link rel="apple-touch-icon" href="assets/logo/hair-id-icon.png">` на `<link rel="icon" type="image/png" sizes="32x32" href="assets/logo/favicon-32.png">` и `<link rel="apple-touch-icon" sizes="180x180" href="assets/logo/apple-touch-icon-180.png">`. Адреса: site/index.html:23-24, site/raboty.html:23-24, site/kak-doehat.html:23-24, site/o-mastere.html:23-24, site/uslugi.html:23-24, site-shop/index.html:22-23, site-shop/katalog.html:21-22, site-shop/nabory.html:21-22, site-shop/kak-vybrat.html:21-22, site-shop/o-mastere.html:21-22. После правки повторить в docs/.

### 12. analytics.js подключён на 10 страницах, но счётчик выключен: код мёртвый

- **Где:** `site/js/analytics.js:25` (направление: производительность)
- **Как ломается:** На любой странице сайта браузер качает и парсит js/analytics.js (5 461 байт) первым в очереди, перед motion.js. Внутри YM_ID = 0 (строка 25) и enabled = YM_ID > 0 (строка 28), поэтому блок подключения Метрики не выполняется, а функция goal() при каждом вызове молча выходит. При этом файл всё равно вешает обработчик на каждый клик по документу и на каждое событие прокрутки — ни одно событие никуда не уходит.
- **Починка:** Правильный вариант: вписать настоящий номер счётчика вместо нуля в /Users/nick/Desktop/Волосы/site/js/analytics.js:25 и /Users/nick/Desktop/Волосы/site-shop/js/analytics.js:25 (`var YM_ID = 0;` → `var YM_ID = <номер>;`). Если счётчика пока нет — удалить строку `<script src="js/analytics.js"></script>` в site/index.html:1266, site/uslugi.html:338, site/raboty.html:286, site/o-mastere.html:189, site/kak-doehat.html:185, site-shop/index.html:384, site-shop/katalog.html:174, site-shop/nabory.html:173, site-shop/kak-vybrat.html:242, site-shop/o-mastere.html:198 и вернуть её вместе с номером. После правки повторить в docs/.

### 13. В docs/ общие ассеты продублированы: 37 одинаковых файлов под двумя адресами

- **Где:** `docs/shop/index.html:311` (направление: производительность)
- **Как ломается:** На GitHub Pages человек читает главную (nekitshit.github.io/hair-id/) и переходит по кнопке «Перейти в магазин ухода» (docs/index.html:860) на nekitshit.github.io/hair-id/shop. Домен тот же, кэш общий, но строка docs/shop/index.html:311 просит /shop/assets/logo/hair-id-mark.png, тогда как главная качала /assets/logo/hair-id-mark.png — байт в байт тот же файл на 288 КиБ качается заново. То же со всеми фактурами и логотипами.
- **Починка:** В сборщике docs/ класть общие ассеты один раз в /assets, а в страницах магазина ссылаться корневыми путями (`/hair-id/assets/...` для GitHub Pages), а не относительными `assets/...`. Дублирующую папку docs/shop/assets после этого удалить через `node /Users/nick/.claude/tools/trash.mjs docs/shop/assets`. Важно: на Netlify сайт и магазин живут на разных доменах (hairid.netlify.app и hairid-shop.netlify.app, см. site/index.html:88,874,1224), там кэш и так не общий — правка касается только сборки docs/.

### 14. У 17 карточек текст «Почему это работает» начинается с висящей точки

- **Где:** `site-shop/js/products.data.js:19` (направление: магазин)
- **Как ломается:** Строки 19, 35, 53, 107, 125, 143, 159, 177, 195, 213, 249, 267, 285, 303, 321, 339, 357 — значение why начинается с «. » («. Идеален для секущихся кончиков…»). Причина в tools/товары-в-магазин2.mjs:44: `const т = (начало + (хвост ? '. ' + хвост : '')).trim();` — при пустом «описание» начало = '' и склейка даёт «. хвост». Карточка печатает это как есть (catalog.js:339, `'<p>' + p.why + '</p>'`). Сценарий: человек раскрывает «Почему это работает» на карточке t001 и первым видит точку. Затрагивает 17 из 36 карточек.
- **Починка:** В /Users/nick/Desktop/Волосы/tools/товары-в-магазин2.mjs строку 44 заменить с
  const т = (начало + (хвост ? '. ' + хвост : '')).trim();
на
  const т = [начало, хвост].filter(Boolean).join('. ').trim();
Затем выполнить node tools/товары-в-магазин2.mjs и пересобрать docs (node tools/сборка-для-github.mjs).

### 15. В разметке для поисковиков (JSON-LD) перечислены 12 демо-товаров, которых в магазине нет

- **Где:** `site-shop/katalog.html:218` (направление: магазин)
- **Как ломается:** Блок `<script type="application/ld+json">` с ItemList: katalog.html строки 213-232, перечислены «Расчёска для влажных волос», «Полотенце из микрофибры», «Сухой шампунь», «Термозащита для волос» и т. д. — это список-образец PRODUCTS из catalog.js:27-185, который на строке 262 перезаписывается живыми данными. В реальном каталоге таких позиций нет: grep по products.data.js по «расчёск» и «полотенц» находит только слово «полотенцем» внутри инструкции «Распылить на подсушенные полотенцем волосы». Тот же блок продублирован в index.html:426, nabory.html:215, o-mastere.html:240, kak-vybrat.html:284 и во всех пяти файлах docs/shop (они побайтово совпадают). Рядом, katalog.html:202, стоит "priceRange": "690–4590 ₽" — при том что цена в каталоге показана ровно у одного товара из 36.
- **Починка:** Удалить в /Users/nick/Desktop/Волосы/site-shop/ блок ItemList целиком: katalog.html строки 213-232, index.html 426-445 (весь второй ld+json), nabory.html 215-234, o-mastere.html 240-259, kak-vybrat.html 284-303, и строку "priceRange": "690–4590 ₽" (katalog.html:202, nabory.html:201, index.html:412, o-mastere.html:226, kak-vybrat.html:270) — либо собирать ItemList из products.data.js в tools/сборка-для-github.mjs. После правки обязательно пересобрать docs (node tools/сборка-для-github.mjs), иначе в docs/shop останутся старые копии.

### 16. У 15 товаров в одной карточке три объёма — выбрать нужный нельзя

- **Где:** `site-shop/js/products.data.js:85` (направление: магазин)
- **Как ломается:** Строки 85, 103, 121, 139, 155, 173, 227 — "volume": "318 / 518 / 738 мл"; строки 281, 530, 548 — «500 / 1000 мл»; строки 440, 458, 476, 494, 512 — «300 / 800 мл». Итого 15 позиций (в заявке сказано 12 — пересчитал, их 15). Карточка печатает строку рядом с ценой (catalog.js:342, `<i>' + p.volume + '</i>`), а в заказ она уходит как есть: lineById (строка 281) кладёт volume в note, и buildOrder (строка 447) собирает «— Реконструирующий шампунь с маслом арганы (318 / 518 / 738 мл) × 1 = 1 500 ₽». Сценарий: мастер получает заявку и не знает, какой флакон человек имел в виду, — переспрашивает по каждой позиции.
- **Починка:** Разделить такие позиции на отдельные карточки по объёму в поставщик/позиции.json (t005-318, t005-518 и т. д.) и пересобрать через node tools/товары-в-магазин2.mjs — тогда объём попадёт и в id позиции корзины, и в текст заказа. Быстрая полумера без правки данных: в /Users/nick/Desktop/Волосы/site-shop/js/catalog.js в буквальном тексте заказа (строка 447) дописать после объёма пометку `(уточню объём)` для позиций, где p.volume содержит '/'.

### 17. Добавление товара перерисовывает весь каталог и схлопывает раскрытые описания

- **Где:** `site-shop/js/catalog.js:437` (направление: магазин)
- **Как ломается:** renderCart() в конце безусловно вызывает renderProducts() (строка 437), а тот заменяет innerHTML всей сетки (строка 329). Сценарий: человек раскрыл «Почему это работает» на трёх карточках, чтобы сравнить составы, нажимает «В заказ» у одной — все три <details> закрываются, потому что узлы созданы заново. Комментарий на строках 428-429 показывает, что для наборов эту же проблему уже обошли точечным обновлением кнопок (строки 430-435), а для товаров нет.
- **Починка:** В /Users/nick/Desktop/Волосы/site-shop/js/catalog.js строку 437 (`renderProducts();` в конце renderCart) заменить на точечное обновление по образцу строк 430-435:
  document.querySelectorAll('[data-add]').forEach(function (b) {
    var id = b.getAttribute('data-add');
    b.textContent = cart[id] ? 'В заказе' : 'В заказ';
    var card = b.closest('.product');
    if (card) card.classList.toggle('is-in-cart', !!cart[id]);
  });
Счётчик #shop-count при этом не трогается — он и так меняется только при смене фильтра (строка 472). Пересобрать docs.

### 18. Страница утверждает, что средств «для всех типов волос» не бывает, а в каталоге такое средство есть

- **Где:** `site-shop/kak-vybrat.html:121` (направление: магазин)
- **Как ломается:** kak-vybrat.html строки 121-122: «Не «для всех типов волос». Такого не бывает: у сухой длины и жирной кожи задачи противоположные» — тот же текст на index.html:249-250. При этом в каталоге лежит t032 (products.data.js:503-504) с названием «Кондиционер с коллагеном для всех типов волос», и это название печатается заголовком карточки. Сценарий: человек читает принцип отбора на «Как выбрать», переходит в каталог и находит ровно то, что ему обещали не продавать.
- **Починка:** Проще всего переименовать позицию: в поставщик/позиции.json у t032 заменить название «Кондиционер с коллагеном для всех типов волос» на «Кондиционер с коллагеном для мягкости и блеска» и выполнить node tools/товары-в-магазин2.mjs. Альтернатива — смягчить формулировку в /Users/nick/Desktop/Волосы/site-shop/kak-vybrat.html:121 и /Users/nick/Desktop/Волосы/site-shop/index.html:249 на «Не «для всех типов волос» вообще, а под задачу из диагностики». Пересобрать docs.

### 19. robots.txt и _headers в собранной копии для GitHub Pages не работают

- **Где:** `docs/shop/robots.txt:4` (направление: магазин)
- **Как ломается:** docs/shop/robots.txt отдаётся по адресу nekitshit.github.io/hair-id/shop/robots.txt. Роботы читают robots.txt только в корне домена (nekitshit.github.io/robots.txt), поэтому строка 4 «Sitemap: https://nekitshit.github.io/hair-id/shop/sitemap.xml» до них не дойдёт. Проверил и второе: корневой docs/robots.txt тоже лежит по /hair-id/robots.txt, то есть не в корне домена, и в docs/sitemap.xml (прочитан целиком) нет ни одного адреса из /shop/ — карта магазина не заявлена нигде. Там же docs/_headers и docs/shop/_headers с X-Content-Type-Options и Referrer-Policy — это формат Netlify, GitHub Pages его игнорирует.
- **Починка:** Карту сайта магазина зарегистрировать вручную в Google Search Console и Яндекс.Вебмастере, либо добавить адреса страниц магазина прямо в /Users/nick/Desktop/Волосы/docs/sitemap.xml (пять <url> с https://nekitshit.github.io/hair-id/shop/...). Про _headers достаточно знать, что на GitHub Pages он не действует; на Netlify (hairid-shop.netlify.app) те же файлы site-shop/_headers и site/_headers работают, поэтому удалять их не нужно.

### 20. robots.txt лежит по адресу, который роботы не запрашивают

- **Где:** `docs/robots.txt:4` (направление: сборка-docs)
- **Как ломается:** Сайт публикуется как проектный, https://nekitshit.github.io/hair-id/. Робот Яндекса или Google запрашивает robots.txt только у корня домена — https://nekitshit.github.io/robots.txt, а это другой (пока несуществующий) репозиторий. Файл docs/robots.txt отдаётся по адресу /hair-id/robots.txt и не читается, вместе с ним не работает строка Sitemap: карты docs/sitemap.xml и docs/shop/sitemap.xml сами по себе поисковику не объявляются.
- **Починка:** Не рассчитывать на robots.txt при адресе в подпапке: после публикации добавить обе карты руками — https://nekitshit.github.io/hair-id/sitemap.xml и https://nekitshit.github.io/hair-id/shop/sitemap.xml — в Яндекс.Вебмастер и Google Search Console. В /Users/nick/Desktop/Волосы/ВЫКЛАДКА-НА-GITHUB.md в раздел «После первой публикации» дописать это пунктом 4, чтобы шаг не считался закрытым.

### 21. Пересборка стирает docs/CNAME — свой домен отвалится после ближайшей правки

- **Где:** `tools/сборка-для-github.mjs:56` (направление: сборка-docs)
- **Как ломается:** Владелец подключил домен через Settings → Pages → Custom domain, как советует ВЫКЛАДКА-НА-GITHUB.md:69-71; GitHub сам коммитит файл CNAME в корень источника публикации, то есть в docs/. Дальше владелец правит текст, делает git pull (иначе push отклонится), запускает команду из раздела «Когда что-то поменяли» (строка 76) — fs.rmSync(docs, recursive, force) сносит папку вместе с CNAME, коммит фиксирует удаление, после push GitHub снимает пользовательский домен, и сайт перестаёт открываться по нему до ручного повторного ввода в настройках.
- **Починка:** В /Users/nick/Desktop/Волосы/tools/сборка-для-github.mjs перед строкой 56 добавить:
const путьCNAME = path.join(docs, 'CNAME');
const cname = fs.existsSync(путьCNAME) ? fs.readFileSync(путьCNAME) : null;
и сразу после строки 73 (запись .nojekyll) добавить:
if (cname) fs.writeFileSync(путьCNAME, cname);
Плюс дописать это предупреждение в ВЫКЛАДКА-НА-GITHUB.md рядом с пунктом 3 раздела «После первой публикации».

### 22. Заголовки безопасности молча пропадут: _headers — формат Netlify, Pages его не читает

- **Где:** `docs/_headers:2` (направление: сборка-docs)
- **Как ломается:** После переезда на Pages файл docs/_headers остаётся файлом с текстом и ничего не настраивает: GitHub Pages не поддерживает пользовательские заголовки ни в каком виде. Referrer-Policy: strict-origin-when-cross-origin, который сайт отдавал на Netlify, отдаваться перестаёт, и заменить его на этом хостинге нечем. Сам файл при этом публикуется как обычный текст по адресу /hair-id/_headers (Jekyll его не съедает, потому что сборка пишет docs/.nojekyll на строке 73), то же у docs/shop/_headers.
- **Починка:** Оставить файлы (пригодятся, если сайт вернётся на Netlify), но в /Users/nick/Desktop/Волосы/ВЫКЛАДКА-НА-GITHUB.md в раздел «Что попадёт в репозиторий, а что нет» дописать строку: «Файлы _headers работали только на Netlify. На GitHub Pages свои заголовки задать нельзя — Referrer-Policy и nosniff там не настраиваются». Если хочется убрать файл с живого сайта, в tools/сборка-для-github.mjs после строки 39 добавить: if (запись.name === '_headers') continue;

### 23. Сборка не проверяет протокол в адресе, а финальный контроль этого не ловит

- **Где:** `tools/сборка-для-github.mjs:10` (направление: сборка-docs)
- **Как ломается:** Владелец копирует адрес из адресной строки браузера (Chrome прячет https:// при показе) и запускает node tools/сборка-для-github.mjs nekitshit.github.io/hair-id. Проверка на строке 10 срабатывает только на пустой аргумент, поэтому замены на строках 61-62 подставляют относительные значения: <a href="nekitshit.github.io/hair-id/shop"> и <link rel="canonical" href="nekitshit.github.io/hair-id/">. Браузер разрешает такую ссылку относительно текущей страницы и уходит на https://nekitshit.github.io/hair-id/nekitshit.github.io/hair-id/shop — 404 на каждом переходе в магазин, плюс битые canonical и og:url. Контроль на строке 83 ищет только подстроку netlify.app и печатает «старых адресов осталось: 0», то есть сборка выглядит успешной.
- **Починка:** В /Users/nick/Desktop/Волосы/tools/сборка-для-github.mjs после строки 8 (после присваивания база) вставить:
if (!/^https?:\/\//.test(база)) {
  console.log('Адрес должен начинаться с https:// — например https://nekitshit.github.io/hair-id');
  process.exit(1);
}

### 24. lastmod главной страницы отстаёт от реальной даты правки

- **Где:** `site/sitemap.xml:3` (направление: сборка-docs)
- **Как ломается:** В карте сайта главная помечена <lastmod>2026-09-07</lastmod>, тогда как site/index.html правился 9 сентября в 20:33, а связанные с ним css и js — 10 сентября. Поисковик, сверив дату, считает, что с 7 сентября страница не менялась, и переобход главной откладывается; остальные адреса в файле помечены 2026-09-09 правильно, отстала именно главная. Сборка даты не трогает, поэтому та же строка приезжает в docs/sitemap.xml:3.
- **Починка:** В /Users/nick/Desktop/Волосы/site/sitemap.xml строку 3 заменить: <lastmod>2026-09-07</lastmod> на <lastmod>2026-09-10</lastmod> (дата последней правки главной), после чего пересобрать docs/ командой из шага 1.

